# Project J -- Active Roadmap
# Read this at the start of every session. Parked/future items live in project_j_backlog.md.
# Tags: [BUG] = confirmed broken | [HIGH] = priority | no tag = SOON/open

---

## BUGS

- Food logging speed sweep -- ALL DIARY PATHS DONE: saveEntry/add (d0873d7), delete-entry + AI estimator log (9f3ba1f). REMAINING (lower priority, NOT the diary hot path): the awaited saveToFirebase in the custom-food / recipe / favorite saves -- add-food.tsx (lines ~541, 1149, 1167, 1187, 1484, 1525) and food-detail.tsx (608 favorites, 1008 my_foods). Same fire-and-forget fix, BUT review each context first -- confirm nothing reads the write result back before making it non-blocking. Less urgent: these are management actions, not the freeze-then-navigate logging moment.
- [BUG] Recovery tab "Text strings must be rendered within a <Text> component" -- red error toast on the Recovery tab in 30D view. Component stack: View > renderHero > SleepHub, so a bare string is rendering outside a <Text> in the recovery hero JSX (app/sleep.tsx renderHero/renderRecovery). Likely a conditional returning a raw string, a stray value/`{' '}`, or a number/`&&` leak that only appears in the 30D branch. Repro: Recovery tab > switch to 30D. Find and wrap/guard the bare string. (From gym thread 2026-06-15.)
- [FIXED 2026-06-16, DEVICE-VERIFIED] Recovery scores on MANUAL-SLEEP / watch-off nights (HRV leaked through the Option B gate). FIX (two matched halves, both pure JS): (1) GATE -- useHealthKit.ts fetchRecoverySignals now holds HRV to the SAME real-overnight standard as RHR: the night's HRV (`hrvWindow`) is only kept when `fetchOvernightRHR(...).asleepMinutes > 0` (real HealthKit asleep sleep-stage segments exist); a manual/watch-off night sets todayHRV=null, so with RHR also null calcRecoveryScore voids (`!hasHRV && !hasRHR`). Real watch-on nights are untouched (tracked sleep -> HRV passes exactly as before). (2) CLEANUP -- sleep.tsx recovery backfill null-branch now ALSO clears stale recoveryScore + recoverySignals (not just restingHR) via read-then-merge, so re-running Backfill erases the ghost scores those nights carried from before the gate. VERIFIED on device: after Backfill re-run, 05-30 / 06-06 / 06-09 / 05-28 dropped out of the dump's recovery list (now sleep-fallback) and gapped on the Recovery trend; watch-on night 06-08 (hrv 54.6 | rhr 52) kept its score unchanged. This is the Whoop/Oura standard (recovery = sleep-derived readout; no wear-during-sleep, no score). UNBLOCKS Recovery-into-Day-Score #6. (Original report below, kept for context.)
  - [ORIGINAL REPORT] Recovery scores on MANUAL-SLEEP / watch-off nights (HRV leaks through the Option B gate). DISCOVERED 2026-06-16 via the new "Dump Day Score w/ Recovery" dev tool. Pattern is exact: every offending night has the fingerprint `hrv <value> | rhr NONE | manual sleep` (e.g. 05-30 hrv 32.5, 06-06 hrv 36.2, 06-09 hrv 54.1, 05-28 hrv 51.2). Justin CONFIRMS the watch was OFF for sleep on those nights (he takes it off to charge at bedtime). What's happening: RHR correctly voids on a manual night (fetchOvernightRHR needs real ASLEEP sleep-stage segments -> none -> NONE, honest gap). But the HRV fetch is NOT held to the same standard: it pulls an HRV sample from the window anyway (almost certainly a STRAY daytime SDNN reading, since Apple Watch logs HRV during the day / Breathe sessions, not just sleep). calcRecoveryScore's Option B gate (`if (!hasHRV && !hasRHR) return NULL`) is then satisfied by that stray HRV, so the night gets a recovery score it should NOT have. So a watch-off night still shows a (often low, since stray HRV) recovery score on the Recovery tab + trend chart. FIX DIRECTION (own session, careful -- live device-verified feature): hold HRV to the SAME real-overnight standard RHR already uses, i.e. only count HRV when the night has real HealthKit sleep stages (tracked sleep), so manual/watch-off nights void cleanly. Likely in useHealthKit fetchRecoverySignals (gate the todayHRV on sleep-stage presence) and/or calcRecoveryScore. After the fix, a backfill re-run must CLEAR the now-stale scores on those nights (the partial backfill-clear I drafted in sleep.tsx was reverted -- redo it together with the gate fix so the void + cleanup ship as one verified change). BLOCKS Recovery-into-Day-Score #6: once recovery voids these nights, the Day Score third category inherits the clean data automatically (it reads pj_<date>.recoveryScore via the fallback ladder), so #6 should wait until this is fixed rather than guarding manual nights twice. The dump tool (HRV/RHR diagnostic line) stays -- it's how we'll verify the fix.
- [FIXED 2026-06-15, DEVICE-VERIFIED] Broccoli Florets duplicate / same-name My Foods opened the wrong entry. TWO root causes, both in add-food.tsx: (1) the My Foods display maps (showMyFoods + myFoodResults) dropped each entry's `id`, so a tapped row carried no identifier and openFoodDetail fell back to NAME matching -> first same-name entry won. Fix: pass `id` through both maps; Edit + Delete now findIndex by id too (with name fallback), so they act on the exact entry. Plus an additive id-backfill in loadMyFoods (assigns an id to any legacy entry missing one, read-then-merge, never alters/removes other fields) so existing data is covered, not just new foods. (2) SEPARATE pre-existing bug surfaced while testing: a genuine FatSecret database result (e.g. "Broccoli Florets · Kroger") was hijacked by a same-named CUSTOM My Food (Great Value) because customNameMatch matched any custom food by name regardless of flags. Fix: gate customNameMatch on `!food.fsId` so real database results are never remapped to a custom food. (Screenshots 2026-06-15.)
- [FIXED 2026-06-15, DEVICE-VERIFIED] Stats Trends "calories" bar graph respawned after delete + duplicated the top calories graph. Root: loadStatsCards re-added ANY default card missing from saved state on every load, so a user-deleted default (graph_calories) came back at the bottom forever. Fix: saveStatsCards now records which default ids are absent from the saved list (= deliberately removed) into pj_stats_removed_defaults on every save, and loadStatsCards skips re-adding those. Done in saveStatsCards (not per delete-handler) so it covers ALL delete paths. New defaults in future updates still appear. statsCardRegistry.ts.
- [FIXED 2026-06-15, DEVICE-VERIFIED] Recipe log: tapping the grams/amount box pushed the page WAY too high. Root: the whole page was wrapped in a behavior="padding" KeyboardAvoidingView, which over-pushes a full scrolling page. Fix: dropped the page-level KAV and moved keyboard handling onto the ScrollView (automaticallyAdjustKeyboardInsets + keyboardShouldPersistTaps + keyboardDismissMode), the correct tool for a scroll page. recipe-log.tsx. (Both By-serving and By-weight verified.)
- [FIXED 2026-06-15, DEVICE-VERIFIED] Streaks modals (Stats tab): handle bar did not close + no haptic. Root: the Manage Streaks and Create Custom Streak modal handles were wired to close-with-haptic but had NO hitSlop, so the 4px strip was untappable. Fix: added hitSlop to both handles. app/(tabs)/stats.tsx.
- [FIXED 2026-06-15, DEVICE-VERIFIED] Macros Today card gear opened a slide-up bottom sheet (banned) + the gear tap had no haptic. Converted to a centered floating modal (handle pill + hitSlop, tap-off + handle-to-close both with haptic, cyan top border accent, scale+opacity animation fired in onShow). Gear tap now fires a light haptic. Also added the macro preset goals INTO the modal (see FOOD & LOG below). app/(tabs)/index.tsx (the macro gear modal lives here, NOT NutritionGearModal.tsx -- that one was already centered).
- [FIXED 2026-06-15, DEVICE-VERIFIED] Workout Completion coach insight was a FALSE POSITIVE -- fired "X of last 7 scheduled sessions ended short" for a cardio-only user. Root: workoutScheduled was `dayType === 'lift' || 'cardio'`, so auto-synced Apple Health cardio days counted as "scheduled sessions." Cardio logs via cardioLogs but never sets the manual cardioComplete flag, so each cardio day read as 0% complete -> rule fired. Fix: workoutScheduled is now lift-only (`dayType === 'lift'`) in both WindowDay builders (smartTipsEngine.ts ~327/527). It's the ONLY consumer of workoutScheduled, so safe; cardio is binary, not a completion rate. Real lift users who leave planned sessions unfinished still get flagged. NOTE for testing: the tip is a STORED smart tip (pj_smart_tips, homeTips card #2+), NOT the AI coach cache -- clear via Dev Tools "Reset Smart Tips Cache" then open Effort vs Results to recompute (engine computes once/day). (Screenshot 2026-06-15.)
- [FIXED 2026-06-15, DEVICE-VERIFIED] Recovery Key Signals 7d/30d sub-labels now follow the range toggle (commit 2ae85ea). The "7d avg" sub-labels + baselines recompute and relabel to "30d avg" on 30D. Confirmed on device: subtext flips when switching to 30D. (Recovery score itself stays frozen on its fixed 7d baseline by design.)
### Fixed
- [FIXED 2026-06-15, DEVICE-VERIFIED] Sleep Coach tip mixed time-window references ("over the last 14 days" + "across all 7 nights" in one tip). Root: the AI was handed BOTH spans at once -- the surface label said "14-day window" (windowDays default 14) while the sleep diagnoses describe a 7-night window, and no prompt rule forced one. THREE fixes: (1) coachAI.ts formatPacketMessage -- sleep surface label changed from "${windowDays}-day window" to "last 7 nights of sleep" so the model is never handed "14 days"; (2) coachAI.ts RULEBOOK -- new "Sleep surface rules" section (parallel to Monthly surface rules) enforcing ONE consistent time reference: sleep tips cite the 7-night window only ("the last 7 nights" / "X of the last 7 nights"); a longer analysis window may only appear via an explicit one-phrase bridge, never as a second unconnected span; (3) smartTipsEngine.ts (Option B) -- the two cross-signal sleep rules (cross_protein_sleep, cross_steps_sleep) had diagnoses hardcoded "Over the last 14 days"; reworded to bake the bridge into the fact ("Over the two weeks we track, on the nights with sleep data..."), since the AI can't alter a fact it's handed. Audited all 7 sleep-set rules: five pure sleep rules cite 7 nights, two cross rules now bridged -- no remaining mixed-window leaks. No tooltip/tutorial window copy to sync (the 14/30/90 mentions are EvR's report selector, unrelated). Pure string changes, hot-reloads. (From gym thread 2026-06-15.)

### Fixed
- [FIXED] Weekly Summary push fired Monday, not Sunday -- scheduler gated on now.getDay() === 1 (Monday) while the in-app weekly summary (utils/weeklySummary.ts) closes the week Sunday (Sun-Sat). Changed gate to now.getDay() === 0 so the push matches the data boundary. services/notifications.ts. OPEN: push fires 11am/12pm (dateSeed-seeded), not true morning -- Justin to decide whether to pull earlier. NOT YET device-verified.
- [FIXED] FatSecret search misses branded compound-word items on singular queries -- "mcdonald nugget" returned generic chicken nuggets instead of McDonald's McNuggets; "mcdonald's fries" apostrophe caused brand mismatch. Two fixes in add-food.tsx: (1) normalizeQueryForApi now strips iOS curly apostrophes and straight apostrophes so "McDonald's" becomes "McDonalds"; (2) fetchFatSecretSearch fires a second parallel query with the last word pluralized (when >= 5 chars and not already plural), merges results secondary-first so the plural results (which FatSecret ranks better for compound words like "McNuggets") surface at top. Short words (< 5 chars) skip pluralization to avoid partial-word artifacts like "nugg" pulling Wendy's Nuggs brand.
- [FIXED] Slow food logging (5-10s on add, double-logging on double-tap, GO_BACK error) -- the saveEntry handler in food-detail.tsx AWAITED saveToFirebase (a secondary Firestore write to the days collection) before navigating back; measured 793ms on WiFi, multiples on weak signal. Metro warm-up on the fresh build exaggerated it to 5-10s. Achievement scans were NOT the cause (momentum 35ms, nutrition 1ms -- gated/early-break). Fix: saveToFirebase now fire-and-forget (data still saves; local write + cloud-backup mirror already persisted it instantly); added savingRef in-flight guard (kills double-log); canGoBack guards on the double router.back (kills GO_BACK). Verified fast on device 2026-06-12. food-detail.tsx.
- [FIXED] Add/Edit Exercise modal crash -- openAddExerciseModal set addExerciseAnim / addExerciseKeyboardOffset, dead names left behind by the scale-animation refactor (bc9f7726). ReferenceError when tapping an exercise to edit. Deleted the two dead lines; Modal onShow already resets correctly. workout.tsx.
- [FIXED] Rest day not overridden by Apple Health workout -- type stayed 'rest' on import so exercises were hidden. Now flips type off rest (to cardio) when a workout imports. workout.tsx import effect.
- [FIXED] YvY streak (vsStreak) always 0 -- pj_vs_streak was read but never written. Now snapshots live result and settles into the streak at day rollover (win extends, loss resets, tie holds; Mindful excluded). index.tsx.
- [FIXED] Daily Goals counters undercount + wrong "last hit" dates -- detection only fired on in-session threshold crossings; if app opened after goal already hit, prevRef was null and first HealthKit update silently skipped. Fixed: also fires when prevRef is null and current value is already at/above goal. Daily gate in handleDailyGoalHit prevents double-counting. Historical counts are permanently low (unrecoverable without data). index.tsx.
- [FIXED] Notification water "behind on water" math inverted -- notifications were scheduled once in the morning when water was 0; copy always asserted "behind" regardless of actual intake. Fixed: water notifications cancel and reschedule on every app foreground with live oz data. Skip-if-on-pace logic now uses real numbers. services/notifications.ts + notificationScheduler.ts + _layout.tsx.
- [FIXED] Notification activity check-in ignores cardio -- condition only checked strength checkmarks, completely ignored cardioLogs; also scheduled at morning with stale data. Fixed: condition now checks both active-cal goal AND exercise-mins goal (both must be hit to suppress). Live refresh on foreground cancels the notification if goals are hit after morning schedule. Copy softened to goal language. services/notifications.ts.
- [FIXED] Today's Verse notification opens Bible to Genesis 1 -- deep link was { route: '/bible' } with no params. Fixed: scheduler reads verse reference (e.g. "Philippians 4:13") alongside verse text, passes as params: { verseRef } so bible.tsx navigates directly to the verse. Verifiable tomorrow morning. services/notificationScheduler.ts + notifications.ts.
- [FIXED] YvY net calories comparison wrong + rounds step numbers -- winCondition compared each day's net against calTarget (consumed budget ~1800) instead of paceTarget (signed net deficit -750). Result: -332 beat -738 even though -738 was essentially on goal. Fixed: import GOAL_DEFICITS from utils/goalHit.ts, compare against paceTarget. Also fixed crash (GOAL_DEFICITS was referenced outside its scope -- now properly imported). Steps format changed from 10.3k to 10,300 for full visibility. Confirmed working via screenshot (6/11 -738 now correctly beats 6/10 -332). head-to-head.tsx. NOTE: YvY streak was poisoned by this bug (days that should have been wins were settled as losses). Streak will self-correct going forward.
- [FIXED] Day Detail haptics missing -- no Haptics import at all. Added light haptics to: nav arrows (prev/next day), calendar icon, calendar picker prev/next/select, all section toggles (sleep/workout/meals/advanced nutrition). Handle bar haptic was missing in index.tsx (stats.tsx already had it) -- fixed to match. day-detail.tsx + index.tsx.

---

## LAUNCH BLOCKERS

- App name + logo -- finalize from shortlist (Prevail, Steadfast, Worthy, Haven, Witness, Sown). Verify App Store + TikTok handle availability. Prevail is strongest.
- App Store Connect setup -- privacy label, age rating, URLs, description, screenshots, review notes. No code. Do after name is locked.
- Verification scan -- production build, device install, all flows confirmed before submitting.

---

## ONBOARDING

- [HIGH] Onboarding full pass (Justin 2026-06-15) -- functionality sweep AND apply the new gradient/aesthetic treatment across all onboarding screens so they match the visual changes made elsewhere. Two parts: confirm every onboarding screen still works correctly end-to-end, and bring the gradient/look current. Dedicated session. app/onboarding/*.
- [HIGH] Post-onboarding mode switch Acknowledgement Modal -- switching style in Settings: brief description + "Keep my layout" vs "Apply defaults." Switching TO Discipline re-fires commitment screen. settings.tsx.
- Mindful onboarding -- encouragement language + Mindful-specific Screen 4 copy. Graph/presets already gated off (your-style.tsx lines 672/762). Remaining: copy pass.
- Macro presets -- Screen 4, Discipline/Balanced only, not Mindful. Also settable in Settings post-onboarding.
- Progress bar on onboarding screens -- segmented step indicator at top of screens 2-7 (not Welcome).
- Apple Health home banner for skippers -- one-time dismissable banner for users who tapped "Maybe later."
- Weight projection graph -- profile page post-onboarding. Onboarding version built; profile page version not. profile.tsx.
- Daily Intention card for Not Right Now users -- Today's Message morphs for NRN: no verse, neutral styling, rotating prompts by style. Low priority.

---

## HOME / UX

- Primary button audit -- app-wide: all primary CTAs to full accent fill. Transparent bordered = secondary only (Edit, Cancel, filter pills).
- Day detail BMR row -- add estimated BMR to calorie breakdown alongside Consumed / Burned / Net.
- Exclusions polish -- first-use callout on calendar dot. Tooltip/help article explaining what exclusions are + effect on Day Score/EvR/streaks. Excluded list view (view + un-exclude all excluded dates). Three entry points: calendar dot, toolkit icon, Settings Help.
- [BUILT 2026-06-14, NEEDS DEVICE VERIFY] Summary pop-up tier system -- on app open, ONE summary pops, highest tier wins: 1st of month -> Monthly, else Sunday -> Weekly, else Day. Lower tiers suppressed for the day (shared pj_last_summary_shown gate). New components/SummaryReadyModal.tsx (tier="week"|"month") mirrors DaySummaryModal (reuses exported ScoreRing): avg composite ring, 3 category pills (avg Nutrition/Activity/Recovery), one context line ("X of N days logged"), FULL BREAKDOWN + GOT IT, no exclude. Precedence wired in index.tsx runScan; weekly/monthly pop-ups fire cancelWeeklySummaryNotification/cancelMonthlySummaryNotification on show (saw it in-app -> no redundant push). Day modal button renamed VIEW HISTORY -> FULL BREAKDOWN, now routes to /day-summary?date= (was Stats archive). Weekly/monthly FULL BREAKDOWN pass weekStart/monthKey params. Reused day_score tooltip on the (i) -- dedicated weekly/monthly tooltip copy is a later option. Disclaimer: weekly/monthly skip the first-use Day Score disclaimer modal (rely on inline micro disclaimer; day-score disclaimer seen in virtually all real cases by the time a week/month closes). REMAINING: device verify; theme audit; Mindful verify.
- [FIXED 2026-06-14] Weekly/Monthly summary PUSH landed on empty screen -- weekly-summary.tsx requires weekStart, monthly-summary.tsx requires monthKey, but the push deep-links carried no params, so taps showed a blank screen. Fixed: services/notifications.ts now passes params: { weekStart: getLastClosedWeekStart() } / { monthKey: getLastClosedMonth() } in the data payload (deep-link handler in _layout.tsx already forwards data.params). NEEDS DEVICE VERIFY (push path).
- [BUILT 2026-06-14] Weekly/Monthly pop-up encouragement lines -- period-aware win line + optional coach note (category avg < 60) on SummaryReadyModal, mirroring the Day modal's two-line block. Pools live in utils/daySummaryCopy.ts periodSummaryLines(): 4 bands (elite/strong/solid/building) x 3-4 variations, mode-aware (Mindful softer, no elite tier), {p} expands to week/month, line picked deterministically by hash of the period key (stable per period, varies across periods, no stored rotation). Faith framing intentionally left out of this pass. REVISIT (Justin): circle back to customize/expand this copy beyond the first pass; consider adding a faith variant (would need to scan faith entries across the whole period).
- [BUILT 2026-06-14] Dev tools: Replay Weekly Summary + Replay Monthly Summary (Settings, next to Replay Day Summary). Sets pj_dev_force_summary='week'|'month'; index.tsx runScan consumes it on open, bypasses the day/date + time + once-per-day gates, loads-or-generates the last closed period, shows the pop-up. Lets the weekly/monthly pop-ups be tested any day.
- [BUILT 2026-06-16, NEEDS DEVICE VERIFY] Weekly/Monthly summary parity pass (Justin flagged W/M repeatedly getting skipped when Day changes land). Brought app/weekly-summary.tsx + app/monthly-summary.tsx up to app/day-summary.tsx parity: (1) UNITS -- Day's Recovery VO2 Max / Cardio Recovery were bare numbers; added "mL/kg/min" + "bpm" to match Week/Month (Month already had them). (2) STICKY HEADER -- W/M headers lived INSIDE the ScrollView (scrolled away); lifted them out to a pinned sibling above the ScrollView with a bottom border, matching Day (content + empty-state both). (3) (i) INFO ICON -- added TooltipIcon to W/M pinned headers (PLACEHOLDER reusing tooltipKey 'day_score'; dedicated weekly/monthly tooltip copy is a later item). (4) CATEGORY-CARD GRADIENTS -- W/M SectionCards were on the old 3px left-accent-border; converted to CardWash (components/GradientCard.tsx) like Day, washed each card with its own category color (Nutrition green / Activity amber / Recovery purple / Weight grey), 0.5 borders, borderTopColor token for cross-theme. (5) COACH INSIGHT -- W/M coach card now matches Day's standard blue accent-tinted box (bg ${accent}12, 1px ${accent}50 border, blue label+sparkles, divider, centered italic body, centered EvR link) in ALL states (Pro/loading/no-data frames also blue-tinted); was a white card with score-tone left bar (green on week, red on month). (6) CALENDAR WASH -- weekly day-strip + monthly month-at-a-glance now carry a gentle scored CardWash in the composite/hero color (gated to accent on no-score). (7) WEEKLY ORDER -- moved Coach Insight ABOVE the day strip so every summary is hero -> Coach Insight -> period detail -> category cards (Monthly/Day already were). (8) WEEKLY COLLAPSIBLE -- day strip is now a collapsible "Week at a Glance" card (chevron toggle, count moved into header) mirroring Monthly's Month at a Glance. tsc clean on all touched files (16 pre-existing errors elsewhere unchanged). REMAINING: device verify all 5 themes (tint legibility on light); dedicated W/M tooltip copy; optional center the loading/no-data coach inner text. NEW STANDING RULE saved to memory: always verify Day Summary changes also land on W/M.
- [DONE 2026-06-16, DEVICE-VERIFIED incl. all 5 themes] Recovery Score into Day Score + summaries (was [NEXT UP] under STATS). Shipped in commit cfcc298. See the #5/#6/#7 sub-bullets below for the full build; theme audit (Light/Dark/Slate/Warm/Blush) passed on the recovery card across Day/Weekly/Monthly. Standing principle saved to memory (feedback_honest_numbers): displayed number must equal the number the engine uses. DECISIONS LOCKED with Justin: REPLACE the composite's 25% third category with the real Recovery Score (NOT blend -- recovery already contains sleep at 22%, blending would double-count); NO floor on the recovery category (it's a calibrated 0-100 readout, flooring blunts a genuine RECOVER day); FALLBACK ladder = Recovery Score if present -> else sleep score (current behavior) -> else category drops (renormalize over Nutrition+Activity, exactly as today on no data). This fallback IS the historical-safety mechanism: any day without a stored recoveryScore stays byte-identical to today, so a version bump only changes the ~30 recovery-equipped days. Backfill 30 days OK (Justin's data only). Summary Recovery section will show the real Recovery Score as the headline number with its factors beneath (add HRV + Activity which actually feed it; VO2/Cardio Recovery stay informational). Build order: dev-tool dump (verify old vs new on real data) -> wire dayScore.ts + buildDayScoreInput + version bump -> section formatting/label -> Mindful + theme audit. Also need a true force-regenerate W/M dev tool (current Replay only loads-or-generates; W/M are write-once snapshots so retroactive data changes don't propagate) -- NOTE: the existing "Regenerate Weekly/Monthly Summaries" dev tools DO clear+rebuild from current data, so #4 is already covered. Detail: SPEC_recovery_coach.md "Future expansion" + this section.
  - [#5 BUILT 2026-06-16, AWAITING JUSTIN REAL-DATA VERIFY] Dev-tool dump + engine seam. (A) utils/dayScore.ts: added optional `recoveryScore` to DayScoreInput + a third-category ladder in computeDayScore (real Recovery Score when present, clamped 0-100 NO floor -> else floored sleep category -> else category drops/renormalize). Two additive outputs: recoveryCategoryScore + recoveryCategorySource ('recovery'|'sleep'|null). sleepScore/sleepDetail left untouched. PROVABLY byte-identical when recoveryScore absent (every live call until #6): thirdScore === old sleep score at the same 0.25 weight. NO version bump, buildDayScoreInput untouched -> live scoring unchanged. (B) utils/dayScoreStore.ts: dumpDayScoreWithRecovery(todayKey, windowDays) -- read-only, walks yesterday back, runs the engine twice per day (old sleep-driven vs new with pj_<date>.recoveryScore injected), returns rows + summary (avg/max delta). (C) Settings > Dev Tools "Dump Day Score w/ Recovery" (Cancel/7d/14d/30d, mirrors Dump EvR Cards), shows per recovery-equipped day: old->new composite, Δ, 3rd-category each way + a summary header. tsc clean (16 pre-existing errors elsewhere unchanged). NEXT after Justin verifies deltas look right: #6 wire buildDayScoreInput to read pj_<date>.recoveryScore + bump DAYSCORE_VERSION 2->3; then #7 section formatting/label + Mindful + 5-theme audit + tooltip/tutorial sync.
  - [#5 VERIFIED 2026-06-16] Justin ran the dump on 14d + 30d. Deltas are all real corrections (avg ~ -4.6, every day moves DOWN because the old sleep-driven third category was floored/inflated into the 90s while real recovery sits 70s-80s; biggest drops land on genuinely low-recovery days). Dump ALSO surfaced the manual-night HRV bug above (a win for the tool). Dump enhanced to show `hrv | rhr | manual sleep` behind each recovery score (the diagnostic that caught the bug); kept. The drafted sleep.tsx backfill-clear was REVERTED (it only fired on truly-void nights; the manual nights are not void since stray HRV gives them a score, so it didn't address the real bug -- will be redone with the gate fix). #6 WAS blocked on the manual-sleep recovery bug -- now [FIXED 2026-06-16, DEVICE-VERIFIED] (see BUGS section). Recovery now voids cleanly on manual/watch-off nights, so #6 will inherit clean data via the fallback ladder. CLEARED TO PROCEED to #6.
  - [#6 + #7 BUILT 2026-06-16, NEEDS DEVICE VERIFY + W/M REGENERATE] The full wire-up + display pass, done in one coherent change (one version bump). SCORING (utils/dayScore.ts + dayScoreStore.ts): (a) #6 -- buildDayScoreInput now reads pj_<date>.recoveryScore live (measured data, NOT frozen into goalSnapshot) and feeds it to the third-category ladder; recoveryScore added to the recompute dataSig so a later recovery change re-rolls the day. (b) WEIGHTS rebalanced (Justin locked) Nutrition 35 / Recovery 35 / Activity 30 (was 40/35/25); CATEGORY_WEIGHTS.sleep key kept (now 0.35) to avoid churn, commented. (c) SLEEP FLOOR DROPPED (Justin: "needs to be honest 100%") -- sleepScoreCategory returns the raw sleep score, no 50-100 band, so "sleep" reads identically on the Sleep tab / Day Score / recovery factor (this killed the 83-shows-as-92 confusion). DAYSCORE_VERSION 3 -> 4 (one recompute migrates all days). DISPLAY (all 5 surfaces, Nutrition -> Recovery -> Activity order everywhere): day-summary.tsx (headline = real Recovery Score, factors HRV/Sleep/RHR/Resp/Prev.Activity, INFORMATIONAL = VO2/Cardio/BloodOxygen; Sleep row shows RAW sleep; reordered cards; weight % auto from CATEGORY_WEIGHTS), weekly-summary.tsx + monthly-summary.tsx (same card; added avgRecoveryScore/avgHRV/avgPrevActivity/avgBloodOxygen to the snapshot schema in weeklySummary.ts/monthlySummary.ts + aggregation incl. prior-day active-cal fetch for the day before the period; hardcoded "% OF SCORE" strings updated 35/35/30; monthly keeps month-over-month deltas on HRV+Sleep), SummaryReadyModal.tsx + DaySummaryModal.tsx (category pills reordered + RECOVERY pill = avgRecoveryScore/recoveryCategoryScore with sleepScore fallback for pre-v4 snapshots; index.tsx passes avgRecoveryScore), stats.tsx archive row (N/R/A order, R = recoveryCategoryScore). TOOLTIP/TUTORIAL SYNCED same session: tooltipRegistry day_score (weights + full recovery rewrite, floor language removed) + data/tutorials.ts ds_* steps (weights, recovery copy rewritten, ds_recovery moved before ds_activity to follow the new card order). MINDFUL: preserved untouched -- category card headlines use the fixed category color (purple), never tier/judgment coloring, in every mode; factors are neutral physiological data; no score bars added. tsc clean (16 pre-existing elsewhere). REMAINING / JUSTIN TO DO: (1) reload + open home so the v4 scan recomputes all days (composites shift: recovery weighted more, activity less, floor gone -- expect a visible drop, it's correct); (2) device-verify the Day Summary card (headline = recovery, raw sleep, order, 35/30 %); (3) run "Regenerate Weekly & Monthly Summaries" dev tools so the snapshots rebuild with the new recovery fields + recomputed composites (write-once, won't update otherwise); (4) 5-theme audit on the new cards. Holding commit until device-verified.
- Day Summary card enhancements -- configurable surface time (fixed at 5am), earlier-access home card option, richer multi-day context. Design session first.
- Day Summaries archive layout -- collapsible rows may get clunky at volume. Revisit once 8+ weeks of real history exist.
- Water modal edit entries -- pencil icon to edit existing water log entries (time/amount).
- Apple sync last sync time -- surface last HealthKit sync timestamp on steps/active cal cards.
- Greeting area customization -- settings picker for top-left home header slot (greeting text, streak badge, calorie line). Design session first.
- Tab bar scroll-to-top -- tapping active tab icon scrolls screen back to top.
- Physical measurements in profile -- waist, neck, hip. Enables Navy method body fat estimate.
- HealthKit permissions audit -- review full list of HealthKit data types vs currently requested permissions. Add high-value missing metrics before next build.
- Loading + error states audit -- sweep all screens for flashy load behavior and silent failures.

---

## FOOD & LOG

- Big 3 macro presets -- quick protein/carb/fat preset picker accessible from the macro gear icon (and/or Settings). Overlaps the Onboarding "Macro presets" item; this is the in-app gear-icon access angle. NutritionGearModal.tsx / settings.tsx.
- HealthKit weight auto-pull -- read Apple Health body mass, ghost value with HK icon, manual entry always wins. Design discussion before building. REQUIREMENT (locked): every pulled entry must show a source badge identifying the originating app/device, and each foreign entry must be individually deletable from within Project J. Manual entry always wins. This is the READ direction (reading weight IN from Apple) -- NOT the write-back build, which only pushes weight OUT.
- %DV entry in Create Food -- bidirectional amount/%DV fields for all nutrients with FDA DV. Full spec in SPEC_nutrition.md.
- Food search fuzzy matching -- My Foods, Recents, Recipes, Favorites use exact substring only. Add fuzzy/Levenshtein for local results. add-food.tsx.
- UNSET button on food detail -- unset barcode-linked food without navigating to Set Foods tab. Needs barcode context in route params.
- SET banner tip -- "(i) Tap SET on the correct item" after barcode scan. On the fence, revisit.
- Calorie target transparency -- (i) tooltip explaining how calorie recommendation was calculated (BMR, lifestyle factor, pace). settings.tsx.
- Date targeting on food log entries -- currently no way to log a food entry to a date other than today from the standard add-food flow. Add date field to time/slot dropdowns for all food logging app-wide. Gap exposed by AI Meal Estimator date/slot targeting design. Separate from estimator build. add-food.tsx.
- [FIXED 2026-06-15, DEVICE-VERIFIED] Recipe creator unit name showed a literal "serving" instead of a dim placeholder. Count already started empty + the per-serving box already gates on a valid count; only the unit-name default needed fixing. servingName now defaults to '' (placeholder "serving, slice, scoop..." shows), falls back to 'serving' on save + in the "Per X" label so no blank labels. recipe-builder.tsx.
- [DONE 2026-06-15, DEVICE-VERIFIED] Macro preset goals in the Macros Today gear modal. Reuses the 4 onboarding presets (High Protein 35/35/30, Balanced 30/40/30, Low Carb 35/25/40, Performance 25/50/25) as centered icon cards (barbell/pie-chart/leaf/flash, matching the Nutrition Goals modal look). Tap applies: writes macroMode='ratio' + the pcts to pj_profile (read-then-merge), writes macroPreset to pj_settings, recomputes the on-screen gram goals live from calTarget, fires a toast. Active preset auto-highlights (matches current pcts; null = Custom). Hidden entirely in Mindful. Modal also has a "Fine-tune in Settings > Goals" pointer that deep-links (?section=goals) to Settings, auto-opens the Goals section, and scrolls to the Macros block (measureLayout after the nav transition). No custom/exact-gram editing in this modal by design (one source of truth = Settings > Goals). app/(tabs)/index.tsx + settings.tsx (CollapsibleSection gained an optional rootRef prop + a section deep-link param).

---

## WORKOUT

- [HIGH] PR tracking + lifting stats -- log PRs per lift, 1RM trend as graphable stat, surface in EvR, volume per muscle group. Required before workout achievement PRs can ship. Planning session needed.
- Workout tab FAB search keyboard dismiss -- no way to dismiss keyboard by tapping outside search bar. workout.tsx.
- Load routine modal polish -- maxHeight + scroll fix done, handle pill + no-X standard applied, exercise preview on select done. Editable/deletable presets are bigger design items for a dedicated session.
- Editable workout note name -- workout notes default to "Workout Note." Should be editable.
- Workout tab muscle group breakdown -- aggregated session-level muscle group summary. MuscleMap exists for individual exercises only.
- Daily exercise goal + active calorie goal progress display -- goals settable + celebration fires, but home screen progress display (rings/bars) not built. Design decision needed: Fitness Metrics card, Activity Rings card, or under Steps.
- HR Zone Training -- 5-zone system, max HR (220-age default, user-settable), optional Karvonen. Stats: time-in-zone stacked bar. Dedicated session.
- Onboarding to home transition -- guided first steps post-onboarding, no cold drop-off. Planning session before building.
- Feedback / bug reporting form -- Settings form: Bug/Suggestion/Other, description, screenshot. mailto deep link to justin.harmke@gmail.com.

---

## STATS & REPORTS

- Fitness Metrics as graphable keys -- VO2 Max, Resting HR, Resp Rate, Blood O2, Body Fat as graph data keys. Graph infra exists; add to DATA_KEY_META + statsCardRegistry + fetchTrendData.
- Trend indicators -- Apple-style up/down arrow on graph values or At a Glance showing change vs prior period.
- Day Score bedtime backfill bug -- backfill-only days score up to 10pts low on Sleep (sleepConsistencyPts defaults 0). NOTE 2026-06-16: the 50pt sleep floor that used to soften this was REMOVED (honesty/consistency, see Recovery-into-Day-Score #7), so it's no longer masked -- but it now only affects the SLEEP-FALLBACK (watch-off) third category, since recovery-equipped days use the real Recovery Score instead. Real fix still lives in calcSleepScore (sleepConsistencyPts should not default 0 for backfilled nights). utils/sleepScore.ts / dayScore.ts.
- EvR refinement pass -- correlations need to be genuinely smart. Hard to test without sufficient data. Revisit alongside Smart Tips.
- [DONE 2026-06-16, DEVICE-VERIFIED] Wire the real Recovery Score into Day Score + summaries. SHIPPED in commit cfcc298 (full detail under the HOME/UX "Recovery Score into Day Score + summaries" DONE entry). Replaced the third category with the real Recovery Score (fallback to raw sleep on watch-off nights), rebalanced weights to Nutrition 35 / Recovery 35 / Activity 30, dropped the sleep floor, rebuilt the recovery card across Day/Weekly/Monthly + both pop-up modals + Stats archive, synced tooltip + tutorial. (Original ask surfaced by Justin 2026-06-16 from a real Day Summary; was sequenced after EvR surface but Justin chose to do it first.)

---

## FAITH

- [DONE 2026-06-15, DEVICE-VERIFIED] Faith gradient pass. Added the shared CardWash (amber, `scored` gentle variant) to: Home Faith Hub card (all 3 pages via the Slide component), and the Faith tab's Today's Message, Bible and Plans, Prayer, and Gratitude Streak cards. LEARNING: amber wash on the already-amber faith bg reads muddy/dark at full strength, so used the gentler scored stop everywhere. ALSO fixed the Home Faith Hub card being darker than the tab cards: switched its page bg from bgCardFaithHero -> bgCardFaith (lighter) and softened its top border from full accentAmber -> rgba(212,134,10,0.38) to match the tab cards. Outer amber glow kept. components/FaithTodayCard.tsx + GratitudeStreakCard.tsx + faith.tsx. (Justin: "I can live with this.")
- [PENDING / TBD 2026-06-16 -- Justin does NOT love the result, MORE CHANGES COMING] Faith card amber top-line removal. Removed the hard amber top border (borderTopWidth 1.5 + borderTopColor rgba(212,134,10,0.38)) AND the solid-amber CardWash first stop from ALL faith cards: home Faith Hub (FaithTodayCard.tsx), Faith tab Today's Message / Bible & Plans / Prayer (faith.tsx), and the Gratitude Streak card faith variant (GratitudeStreakCard.tsx). Replaced the wash with a soft translucent amber LinearGradient (accentAmber `2E` -> `00` over 64px, no hard top line) so the warm fade stays but the hard top edge is gone. Non-faith Gratitude variant left untouched (keeps the blue CardWash + 1.5 accent). Commits b4fae9b (home hub), f9be33c (faith tab + gratitude). STATUS: in-progress -- Justin is not happy with the current look and has more faith-aesthetic changes to make. Do NOT consider the faith card aesthetic done; revisit this whole treatment next session.
- [TBD / DISCUSS -- no solution locked] Faith tab inner-tile contrast + color polish. Inner tiles within Bible and Plans (reading-plan tiles, devotional tiles, Continue Reading, Find something to read) have insufficient contrast from their parent card on BOTH themes (light: beige on beige; dark: dark on dark) so they don't read as distinct tappable elements. Broader issue: amber is overused across all faith elements, flattening the hierarchy; likely needs a dark counterpart on light theme. Discussion needed before any build. (From gym thread 2026-06-15.)
- [TBD / DISCUSS -- decision needed before building] Faith tab background / overall aesthetics. Something feels off, not warm enough. Three options on the table: (a) subtle warm background tint, (b) top gradient wash, (c) lean harder into card warmth. Pick a direction before any build. Related to the inner-tile contrast item above. (From gym thread 2026-06-16.)
- Today's Message / Faith Today home card -- center the dot page indicators horizontally at the bottom of the card. components/FaithTodayCard.tsx. (From gym thread 2026-06-16.)
- Today's Message overhaul -- user-controlled pool (preset KJV + custom), static vs cycle toggle, pool management in Settings, NRN card hidden by default, custom scripture picker (book > chapter > verse, up to 4). Dedicated session.
- Cycling Bible verses -- fine-print at bottom of Log tab and Workout tab only. Rooted: on. Exploring: optional. NRN: hidden.
- Plans hub browsing -- category grouping (wellness/body, heart/identity, peace/comfort, "Need a Word Right Now"). Filter chips, search. app/plans.tsx.
- Bundle full KJV offline -- today fetches from GitHub, breaks offline. Bundle ~4MB into app. data/bible-web.ts.
- Achievement toast remaining -- trigger context under achievement name, wording update before App Store launch.
- Theme unlock starter challenge -- code done (Slate/Warm/Blush unlock together). Challenge content/trigger TBD.
- Bible translation selector -- translation picker in Bible gear modal. KJV only today.
- Faith AI verse-banner share tap-through -- tapping outside iOS share sheet highlights random verse underneath. Cosmetic/low priority.
- Challenges/Missions layer -- full spec in SMART_COACH_SPEC.md. Parked behind Faith AI track.
- Donate/Support button -- post-TestFlight. StoreKit tip jar or Ko-fi. Not urgent.

---

## SLEEP

- [BUILT 2026-06-16, DEVICE-VERIFIED] RHR overhaul: compute our OWN overnight RHR instead of Apple's daytime HKQuantityTypeIdentifierRestingHeartRate. METHOD (validated on device against Apple as the sanity reference, landed 52=52): new fetchOvernightRHR(anchorDate?) in useHealthKit.ts queries raw HKQuantityTypeIdentifierHeartRate across the sleep window, keeps samples inside ASLEEP stage segments (core+deep+rem, awake excluded), and takes an ARTIFACT-TRIMMED ROBUST LOW (drop bottom ~2% sensor dropouts, average the ~2nd-12th percentile band) -- NOT deep-only (too narrow, only ~16 of 908 samples, overshot to 58) and NOT raw min (artifact-prone, a 40 bpm dropout). No new HealthKit permissions (raw HeartRate already authorized). WIRED APP-WIDE: (1) recovery score daily RHR (fetchRecoverySignals, live + historical backfill); (2) recovery RHR BASELINE now averages our stored overnight RHRs over the prior N days from pj_<date>.recoverySignals.rhr (was Apple's 7-day discreteAverage = inflated 63 vs the real ~52, which exaggerated the delta) with an Apple-query cold-start fallback; (3) fetchTodayData restingHR (home fitness card + the pj_<date>.restingHR that stats graphs + weekly/monthly summaries read); (4) EvR already used recoverySignals. NO-DATA NIGHTS gap (null), never a fake number -- manual/watchless nights return null and the backfill CLEARS any stale top-level restingHR so all surfaces gap consistently (the awake-HR fallback that produced 80-90 spikes was removed; the watch-worn-manual-sleep case that produced ~60 spikes is now cleared too). Backfill writes restingHR UNCONDITIONALLY per tracked day (the old score-change gate skipped recent frozen days and left them on Apple values). Drilldown chart now shows "X of Y nights tracked, Z without data". Dev tool "Dump RHR" added (last night: our value vs Apple vs distribution). REMAINING: (a) drilldown "HOW IT'S CALCULATED" copy + tooltips/tutorials to state the robust-low method exactly; (b) going-forward manual-night edge -- the home path keeps the prior RHR rather than gapping, so a future watch-worn-manual night could write a stale value (rare; clean up in fetchTodayData/index save); (c) stats older than 30 days still show old Apple RHR (backfill reach). User re-runs Backfill once to repopulate the 30-day history with the new method. (From gym thread 2026-06-16; built + device-verified same day.)
- [FIXED 2026-06-15, device-verified] Recovery hero delta arrows contradicted their numbers (green up-arrow next to "-1 bpm" on resting HR, red down-arrow next to "+0.3" on resp). Arrow was driven by isPositive (good/bad) instead of change direction. Fixed: arrow now follows the delta's sign (down for negative, up for positive); color still carries good/bad (green isPositive / red not). One line, app/sleep.tsx ~987. Same principle as the earlier Sleep Metrics neutral-delta fix.

- [FIXED 2026-06-15, FREEZE DEVICE-VERIFIED / OPTION-B VOID NOT YET] Recovery Score drifts during the day -- now a FROZEN MORNING SNAPSHOT (Whoop/Oura model). VERIFIED 2026-06-15 via new "Reset Today's Recovery Score" dev tool: score recomputed to 75 STEADY and held; Resting HR row now shows the overnight average (62 bpm, -1 vs baseline) instead of a higher daytime read. NOT yet verified: the Option B void/empty-state (needs a watchless / no-overnight-data night to trigger). Justin saw 82 -> 80 between 6:11am and mid-morning (HRV 50.9 -> 48.8, RHR 52 -> 53) while sitting still. THREE changes shipped: (1) SNAPSHOT ACCURACY (useHealthKit.ts fetchRecoverySignals) -- the LIVE path's RHR and Resp now use the OVERNIGHT sleep-window average (sleepStart->sleepEnd, matching HRV) instead of getMostRecentQuantitySample, so daytime readings can't drag the score. NOTE: deliberately used the sleep-window for live RHR (NOT the backfill branch's full-day average, which would still drift during the day); no daytime fallback when the window is empty (the formula renormalizes around dropped signals). (2) FREEZE (app/sleep.tsx) -- new frozenScore state loaded from pj_<date>.recoveryScore on mount; persist effect rewritten to LOCK-ONCE (first score of the day is written and NEVER overwritten; if one already exists, reuse it); hero headline + label + zone now render the frozen value via new recoveryZone() helper (utils/recoveryScore.ts, single source of truth for label/zone so frozen and live agree). Signal ROWS (HRV/RHR values) still render live but barely move now that they're overnight averages. (3) OPTION B GATE (utils/recoveryScore.ts, Justin chose B 2026-06-15) -- Recovery is now a physiological readout: the minimum-data gate requires a real overnight signal (HRV or RHR), so a manually-typed sleep duration ALONE no longer manufactures a score; it voids to the empty state (Whoop/Oura void-without-wear behavior). Sleep still counts as a weighted input whenever overnight physiology is present, and a manual duration + real watch vitals computes normally. Empty-state copy reworded ("Recovery needs overnight heart data from your Apple Watch..."). NO data loss: past stored scores untouched; manual-only days just stop writing new scores. Stale RHR drilldown copy synced (data/metricDrilldowns.ts: "day's resting value" -> overnight window). tsc clean on all touched files (useHealthKit.ts, sleep.tsx, recoveryScore.ts, metricDrilldowns.ts). Pure JS, hot-reloads. EDGE (accepted, rare): if morning signals later drop to null mid-day the hero shows the empty state even with a frozen score, but overnight baselines stay readable all day so recoveryResult should not go null after a data-present morning. VERIFY: open Recovery in the morning, note the score, recheck hours later -- it should hold; manual-sleep-only / watchless day should void with the new empty-state copy.
- [DROPPED 2026-06-15] Sleep score 4th tier ("Solid Night"). Decided AGAINST after working it through: there are only 3 status colors (green/amber/red, per theme in theme.tsx), so a 4th tier forces a doubled color. Justin rejected every compromise -- two greens (confusing), accent/cyan for the top (muddy), and lowering green to 78 (green/"Well Rested" stays a high bar; 80 is his absolute floor and even that he's hesitant on). A clean 4th tier would require a NEW in-between token (gold/lime between green and amber) across all 5 themes; not worth it. Keeping the clean 3-tier (>=85 Well Rested green / >=70 Could Be Better amber / <70 Poor Sleep red). 
  - FINDING worth keeping (surfaced during this): the sleep-score label+color BANDS are hardcoded independently in 4 places -- app/sleep.tsx (hub hero, statusGood/Warn/Bad), app/(tabs)/index.tsx (home Sleep card; also shimmer at >=85 + SLEEP_TIPS.good at >=85), app/day-detail.tsx (uses DIFFERENT tokens: accentGreen / hardcoded #d4860a / accentRed, so its green may not match the hub), components/StatsGraphCard.tsx ("Well Rested Nights" count = score >=85). Plus tooltipRegistry.ts + data/tutorials.ts reference the band names. There is NO shared helper. If the bands are EVER revisited, FIRST centralize into one helper (utils/sleepScore.ts: sleepScoreLabel(score) + sleepScoreColor(score, theme)) and point all 4 screens at it -- otherwise any change drifts across screens. Offered the centralize cleanup this session; Justin deferred it.
- [EvR DONE 2026-06-15, NEEDS DEVICE VERIFY; summaries/Day Summary/Stats still OPEN] Recovery in summaries / EvR -- the dedicated Recovery Coach is HUB-ONLY. Sleep already feeds the holistic home/EvR/weekly/monthly coaches via general sleep rules; RECOVERY does not yet. Recovery is a headline signal, so summaries should reference it ("recovery averaged 74 this week"). Approach (locked): feed recovery as an INPUT to the existing summary/EvR coaches, do NOT duplicate the recovery-tab tip.
  - EvR SHIPPED 2026-06-15: graduated R1/R2/R3 of the Recovery coach into the EvR rule pool as 3 standard cross-signal candidates -- rec_load_drag (recovery dips after hardest days), rec_tracks_sleep (recovery lower after short nights), rec_sustained_low (sustained under-recovery while training steady). Family 3 (cross-signal tier), scenarios 3.10/3.11/3.12. Reuses the hub coach's exact R1/R2/R3 math via parallel compute helpers (computeRecLoadDrag/TracksSleep/SustainedLow in smartTipsEngine.ts) so the shipped hub coach is UNTOUCHED (chose duplication over refactoring a live feature). Gated on >=10 recovery days in the 14-day window (watchless users never fire). New RULE_COPY entries in smartTipsCopy.ts (3 variants + Mindful each). rec_load_drag/rec_tracks_sleep are EDUCATIONAL tone, rec_sustained_low corrective. Mindful suppression inherited automatically. Snapshot/acute/no-cause recovery rules stay hub-only by design. Smart Tip tooltip + EvR patterns tutorial synced. tsc clean on touched files. KNOWN/ACCEPTED: hub Recovery coach and EvR can both land on a recovery finding the same day -- identical to how sleep rules already live in both the hub Sleep coach and EvR. VERIFY: need 10+ recovery days; Settings > Reset Smart Tips Cache, then open Effort vs Results. FINDING 2026-06-16 (track-2 gap, NOT a data problem): these rec_* rules live ONLY in the OLD EvR coach pool (smartTipsEngine candidate set). The NEW track-2 card engine (buildDiagnosticCards in utils/diagnosticReport.ts, commit 8ea3464) is a separate from-scratch model with its own hardcoded card builders + a fixed correlation set (sleep->nextday-cals, highburn->nextday, weekend, water->cals, sodium->weight, steps->sleep) and contains ZERO recovery references (grep-confirmed). So the "Dump EvR Cards" feed shows no recovery card no matter the data. The SPEC ("What stays") explicitly lists recovery load-drag/tracks-sleep/sustained-low as SEED correlations for the feed, so this is an unfinished port, not a design choice. ACTION: when the EvR surface is rebuilt (track 2 step 2), PORT rec_load_drag/rec_tracks_sleep/rec_sustained_low into buildDiagnosticCards as proper DiagnosticCards (claim/proof/lever + strength/severity + per-pattern window + Mindful copy each), or recovery findings will disappear from EvR when the surface switches to the new feed. The old-engine rec_* path becomes moot once the surface uses the card feed. [PORTED + VERIFIED 2026-06-16] computeEvrRecoveryFindings()/dumpEvrRecoveryDebug() added to smartTipsEngine.ts and wired into buildDiagnosticCards; dump footer confirms the rules now evaluate (12/10 recovery days) and correctly fire nothing on stable recovery (mean 77 > 70 floor).
  - STILL OPEN: wiring Recovery Score into weekly/monthly summaries, Day Summary, Stats, and dayScore.ts. Detail in SPEC_recovery_coach.md ("Future expansion").
- [BUILT both tabs 2026-06-15, NEEDS DEVICE VERIFY] Metric Drill-Down SLICE 2 -- per-metric history mini-graph INSIDE the drill-down modal, following the universal 7D/30D toggle, so tapping a metric shows its trend over the selected range. SHIPPED (recovery metrics): new MiniMetricChart in components/MetricDrilldownModal.tsx (auto-scaled y-axis, area+line+dots, tap a point to read its date+value in a header line; width via onLayout since modal width is dynamic). MetricDrilldownData gained `history` (one point per day/night) + `chartColor`. DATA SOURCE DECISION (resolves SPEC 13.5/13.8 pending): read from LOCAL storage, NO new HealthKit query -- recovery signals (HRV/RHR/resp/SpO2) + activity come from pj_<date> (the freeze + Backfill Recovery History already persist recoverySignals/activeCalories), sleepScore from the in-memory fetchSleepHistory; so fetchSignalHistory stays PARKED (not needed). buildMetricHistory(key, days) in app/sleep.tsx loads the series in an effect on drillKey/range change; returns null for unwired keys (no false empty state); <2 points shows a "trend builds over time" state. MINDFUL: chart shows in all modes (factual); growth-off neutralizes chartColor to accent (matches the hero). POLISH (Justin device catch 2026-06-15): (1) y-axis now uses round-number ticks (modal copy of niceYTicks WITHOUT the 2.5 step, so whole-number metrics like Sleep Score/RHR/activity get whole-number axes, not 99.6/65.7 decimals); (2) today's point was missing on the heart-signal graphs (HRV/RHR/resp/SpO2) when today's score was frozen before per-night signals were persisted -- now falls back to the live hero values for today so the graph stays continuous + matches the hero (self-heals going forward). Prev. Activity intentionally may end at the last complete day (today is partial; recovery uses yesterday's activity). SLEEP-TAB METRICS DONE 2026-06-15: buildMetricHistory extended to deep%/REM%/bedtime/sleepBalance/wakeEvents from filteredHistory (in-memory sleep history). Two need custom y-axis labels, threaded via a new optional chartValueFormat on MetricDrilldownData -> formatTick on MiniMetricChart: bedtime plots clock times (fmtBedMin), sleep balance plots nightly sleep DURATION in hours (label fmtMs, y suffix 'h'). deep/REM plot %, wake events plot count. REMAINING: device verify both tabs (run Backfill first so recovery signal graphs have depth); theme audit; Mindful verify. [BUG, device-confirmed 2026-06-15, NOT fixed yet] Bedtime drill-down y-axis labels are CLIPPED on the left edge ("1:00 AM"/"12:20 PM"/"9:40 PM" cut to ":00 AM"/":20 PM"/":40 PM"). Cause: MiniMetricChart PAD_L=32 is too narrow for clock-time labels (fine for short numeric ticks). Fix: widen PAD_L (and/or shrink y-label font / right-anchor with more room) when chartValueFormat produces wide labels -- e.g. bump PAD_L to ~46 for bedtime, or measure. Only bedtime is affected (other metrics' numeric labels fit). components/MetricDrilldownModal.tsx MiniMetricChart. ORIGINAL NOTE: Applies to BOTH tabs now that Slice 1 is on Recovery AND Sleep -- build it ONCE in the shared components/MetricDrilldownModal.tsx so both get it at the same time (do NOT wire twice). Plumbing needed: a light day-bucketed history query in useHealthKit (fetchSignalHistory) for the recovery signals HRV/RHR/resp/activity/SpO2 (these have no daily-bucket fetch yet); sleep metrics already have most history via fetchSleepHistory (deep%/REM%/bedtime/wake/balance per night). Pure JS, no rebuild. DESIGN PASS FIRST before building: sparkline vs full mini-chart, legibility at modal size, axis/labels, how the selected-range value reads against the trend. Spec: SPEC_sleep.md Section 13. (Justin wants this; doing Recovery Coach wiring first 2026-06-15.)
- [DONE 2026-06-15, device-verified] Sleep tab metric-row drill-downs -- Sleep Metrics card rows (Avg deep / Avg REM / Bedtime / Sleep balance / Avg wake events) are now tappable and open the same MetricDrilldownModal as Recovery (chevron affordance + haptic, identical layout). 5 new registry entries in data/metricDrilldowns.ts (deep/rem/bedtime/sleepBalance/wakeEvents, behavioral non-clinical copy). Refactored the Sleep Metrics computation into a shared `sleepMetricRows` useMemo (mirrors recoveryResult) so the card display AND the drill read the SAME numbers (display kept pixel-identical); each row carries dkey/isGood/reference. buildDrill now handles sleep keys WITHOUT requiring recoveryResult (works on a watchless day). No-data ('—') rows are intentionally non-tappable. Mindful tip-gating NOT applied here, matches Recovery's current behavior (deferred Slice 3 covers both tabs). commit effbf97. REMAINING for this drill system: Slice 2 (per-metric mini-graph, see below) + Slice 3 (Mindful pass + theme audit).
- [OPEN -- TUNE + REVISIT R5] Smart Recovery (and sleep) coach refinement (Justin 2026-06-15). The Recovery Coach is already BUILT (line below) but thresholds are first-pass strict guesses. Two asks: (1) tune the pattern thresholds against real output so the pattern rules (R0-R4/R7/R9) actually surface, not just the R8 snapshot floor; (2) REVISIT the omitted R5 ("too sedentary / under-training") -- Justin's example: recovery 90+ for ~7 days straight likely signals activity load is too LOW, a real coachable lever. R5 was omitted in SPEC_recovery_coach.md (low confidence, risked contradicting R1's rest message) -- reconsider now that we can watch real data; if added it must not co-fire with R1. Sleep coach is already engine-wired; lower priority to deepen. Decision/tuning pass, not a fresh build.
- [BUILT 2026-06-15, PARTIALLY VERIFIED + NEEDS THRESHOLD TUNING] Smart Recovery Coach. Shipped per SPEC_recovery_coach.md (commit f5ee088). DEVICE-VERIFIED 2026-06-15: AI voice reads naturally (snapshot path, R8/rec_snapshot_hrv); offline = deterministic fallback shows (recoveryCoachTip HRV branch, no blank/error); dark theme good; MINDFUL softening confirmed (after Replay-while-in-Mindful the corrective tip neutralizes to "Something worth noticing... a solid place to be," no directive/scoring nudge). NOTE: tip caches once/day, so a mode switch needs a Replay to recompute same-day. STILL TO VERIFY (data/time-dependent, not blocking): pattern rules R0-R4/R7/R9 (Justin's flat mid-70s data only triggers the snapshot floor so far -- patterns surface with shifting data or threshold tuning); full 5-theme audit (dark confirmed); Option B-style empty state (needs a watchless night). THRESHOLDS are first-pass strict guesses -- tune against real output. Engine: WindowDay now carries recoveryScore/recoverySignals/excluded; new computeCoachPacketRecovery (smartTipsEngine.ts) runs the pattern brain over the window (R0 acute drop, R1 load-drags-recovery, R2 recovery-tracks-sleep [picks stronger of R1/R2], R3 sustained-low/deload, R4 bedtime, R7 strong+stable, R8 snapshot floor, R9 honest no-cause) and builds a CoachPacket. coachAI.ts: 'recovery' surface + "Recovery surface rules" prompt (behavioral-not-clinical, no inventing causes, one window per tip) + refreshCoachTipRecovery, cache pj_coach_tip_recovery. app/sleep.tsx: lazy compute on Recovery tab open passing the live snapshot; card shows AI tip via resolveTipBody with deterministic recoveryCoachTip as instant/offline fallback; Mindful corrective suppressed (engine neutralizes the AI path; card hides deterministic corrective when no cache). Replay Recovery Coach dev tool added. tsc clean on all 4 files. THRESHOLDS ARE FIRST-PASS STRICT GUESSES (REC_PATTERN_DELTA 6, ACUTE_DROP 12, LOW_MEAN 70, BEDTIME_SD 60, MIN_PATTERN_DAYS 10) -- tune against real output. v1 SIMPLIFICATION: no cross-day rule rotation (cache once/day + AI rephrases via previousTip mitigates nagging); a persistent real pattern legitimately repeats. TEST: run Backfill Recovery History (now also writes daily signals) -> Replay Recovery Coach -> open Recovery tab. Verify AI voice, that the tip reflects a real pattern/honest-no-cause, Mindful behavior, and offline fallback. CONFIRMED the current tip is the LOCAL deterministic recoveryCoachTip (app/sleep.tsx:420), NOT Smart Coach AI; engine has zero recovery signals (smartTipsEngine.ts line 2356 literally "Recovery (HRV/RHR) is a future surface"). Justin wants it GENUINELY smart -- actionable "why am I stuck in the 70s / how do I improve" tips, not AI-painted templates. Full design in SPEC_recovery_coach.md: hybrid pattern-detection coach that correlates daily recovery score vs the user's logged sleep/training-load/bedtime/nutrition over a window and surfaces the dominant lever (R1 training-load-drags-recovery, R2 recovery-tracks-sleep, R3 sustained-under-recovery/deload, R4 bedtime-inconsistency, R7 strong+stable, R8 snapshot floor; R5/R6 deferred). Shared infra: persist frozen daily HRV/RHR/resp/SpO2 with the recoveryScore freeze (read-then-merge) -- feeds BOTH this coach AND Slice 2 graphs. Build = engine-style (computeCoachPacketRecovery + recovery rules) mirroring computeCoachPacketSleep, AI voice via a new 'recovery' surface in coachAI.ts, deterministic fallback kept. OPEN DECISIONS (in spec): 7 vs 14-day window (rec 14); persist daily signals (rec yes); v1 rule subset; confidence strictness. Sleep coach parity: already engine-wired (sleep+cross rules), leave as-is for now, revisit deepening after recovery proves the approach. (From gym thread 2026-06-15.)
- [TBD / DISCUSS -- no solution locked] Sleep Metrics card color grading -- revisit. All right-column values are color-coded incl. REM/Deep vs healthy range, but sleep stages are fluid/body-regulated so static range grading can mislead (e.g. high REM after a stressful week isn't actually a problem). Idea: move metrics to context-only display (no color grading), but that conflicts with the all-colored right-column design. No clear solution; discussion needed before any change. (From gym thread 2026-06-15.)
- [BUILT 2026-06-15, BALANCED + ALL 5 THEMES DEVICE-VERIFIED; POLISH PASS IN PROGRESS] Recovery hero redesign -- richer hero replacing the plain score number + change rows. Final design = HYBRID (donut + divergent bars, not either/or): (1) reuses the exported components/DaySummaryModal ScoreRing as the score donut (animated arc, colored by recovery zone), zone word (PRIMED/STEADY/RECOVER) + Limited-data badge beside it; (2) each signal row got a DIVERGENT BAR (new DivergentBar component in app/sleep.tsx). KEY INSIGHT that made divergent work cleanly: every RecoveryComponent already carries a 0-100 sub-score where 75 = at-baseline, so the bar uses `|score-75|/25` (symmetric +/-25 window) as magnitude and `score>=75` as direction -- center = baseline, fills RIGHT/green when the signal is HELPING recovery, LEFT/amber-red when DRAGGING. The literal up/down stays in the number+arrow on the right (decoupled: color = good/bad, arrow = raw direction), so e.g. resting HR -10 bpm reads as a green right-fill, not a confusing green-pointing-down. This handles all five signals with NO special-casing: sleep (composite, no delta -> bar from its score, no arrow, already had none), and activity (two-sided actScore caps at 75 so it only ever sits center or drags left, which is correct). Inline legend line added ("Each bar shows today vs your 7-day baseline. Right is helping, left is dragging."). MINDFUL: separate branch -- no donut, no number, no bars, no judgment color; soft `contrast` phase glyph + calm zone word (Recovered/Steady/Take it easy) + one-liner + signals as neutral muted text (still tappable to drill). Scope was hero ONLY (heroCard in renderRecovery); Trend / Key Signals / Coach cards untouched; SpO2 informational row kept as-is. tsc clean (only pre-existing unrelated errors in other files). Pure JS, hot-reloads. POLISH PASS (Justin device feedback 2026-06-15, doing one at a time): (a) SpO2 row DONE -- was a plain mismatched row (left pill, muted label, no viz); SpO2 is absolute-range not baseline-relative, so it got a new RangeBar (new component in app/sleep.tsx: fill on a 90-100 scale, green at/above 95 / amber below, faint threshold tick at 95) and the row now matches the divergent rows' column layout + text color. (b) DONE: folded each signal's baseline into the hero rows (subtext "Base XXms" under the label, from the frozen 7d baseline that the deltas/bars compare against; Sleep Score has none) and RETIRED the Key Signals card (removed the card, its now-dead rangeBaselineSignals state, and the range-fetch effect). Justin OK'd the two minor losses: the 30D per-signal baseline view (30D now drives the Trend chart only; the universal toggle STAYS -- it is shared with the Sleep tab and will also drive Slice 2 drill-down graphs) and partial-signal visibility on a void/watchless day. Recovery tab is now hero + trend + coach. BASELINE WINDOW decided: 7d, FIXED (not adjustable -- footgun; Whoop/Oura don't expose it), correct for an acute readiness score; the 14d longer view lives in the Recovery Coach. Hero legend keeps the "vs your 7-day baseline" disclaimer. ROW-CONSISTENCY FIXES (Justin device catch 2026-06-15): (1) the per-row directional ARROW was REMOVED -- it fought the divergent bar (e.g. resp went up = red up-arrow, but the bar correctly drags LEFT, confusing) and its color used a different good/bad rule than the bar (activity's "within 30% = good" gave a GREEN down-arrow sitting on an amber dragging row). Now the bar carries helping/dragging direction and the delta TEXT is colored by the same score-based good/bad as the value (green/amber/red), so every row tells one consistent story; the +/- sign in the delta still shows raw direction. (2) HRV delta now shows 1 decimal (recoveryScore.ts) so it reconciles with the 1-decimal value + baseline (was "-1ms" next to 47.1/48.2; now "-1.1ms"). PARKED (separate formula decision, NOT changed): activity is TWO-SIDED (actScore drags for any deviation from your norm, so a lighter day reads as a mild drag, not a win) -- debatable for recovery (a rest day arguably should help), but changing it shifts everyone's scores, so revisit deliberately later. (c) DONE: MINDFUL redesign. Mindful + growthAreas ON now FALLS THROUGH to the full hero (donut + bars) -- fixes the miss where growth-on users were wrongly stripped (gate changed to `styleMode === 'mindful' && !mindfulGrowth`). Mindful growth-OFF keeps the soft hero but is no longer dull/grey or a Key Signals clone: ACCENT color (theme.accentBlueRaw) on the contrast glyph + zone word, and the divergent bars now render via a new `tone` prop on DivergentBar (single flat accent color, no green/amber) so they show where each signal sits vs baseline as OBSERVATION not a verdict; values/deltas neutral (textSecondary/textDim), baseline subtext shown, observational legend ("Where each signal sits next to your 7-day baseline"). SpO2 intentionally omitted from the Mindful card (informational-only, keeps it calm). tsc clean. THEMES: all 5 audited on the recovery card, look fine (Justin 2026-06-15). "OUT OF 100" under the donut: KEEP (Justin -- only dropped it on the Sleep hero because that score is not in a ring and it broke vertical centering). Limited-data day: not testable right now (no recent no-HRV night), Justin to spot-check when one occurs. FOLLOW-UP (flagged, not built): a dedicated recovery-hero (i) tooltip explaining the divergent viz (inline legend covers it for now). (Design discussion + build, gym-chat draft, 2026-06-15.)
- [BUILT 2026-06-14, device-gauged] Metric Drill-Down SLICE 1 (Recovery metrics). Tap any Recovery row (hero contributor, SpO2, or Key Signal) to open a focused modal: header (metric title + live value in its status color + status word + baseline/range reference), then boxed sections What it is / How it's calculated / What it affects, and a "How to improve" callout. New components/MetricDrilldownModal.tsx (centered, scale+opacity, handle pill, mirrors NutrientDrilldownModal) + new data/metricDrilldowns.ts registry (hrv/rhr/resp/activity/sleepScore/spo2; double-quoted copy so apostrophes are safe). SMART TIPS: improve tips are deterministically SELECTED from the user's real standing (isGood from the live contributor) so a below-baseline HRV gets "prioritize sleep, keep today easy" and an at/above one gets "you're recovered, train hard." SpO2 is informationalOnly (no prescriptive tips, doctor disclaimer). sleep.tsx: rows are now TouchableOpacity with a chevron affordance; buildDrill() assembles registry content + live value/status/reference; openDrill sets drillKey. VISUAL ITER (device): top accent is a real top BORDER (4px, status-colored, follows rounded corners -- a separate strip child poked square corners); section boxes take a LIGHT 8% tint + left accent of the metric's OWN status color (amber/green/red), matching the data point (bgProgressTrack solid grey was too dark, cyan accent didn't match data); "How to improve" stays cyan as the actionable standout. ALSO FIXED: SpO2 value + bar in the Today's Recovery card now go green at >=95 (was neutral) to match the modal. REMAINING (Slice 2): per-metric history mini-graph (needs a light fetchSignalHistory daily-bucket query in useHealthKit). Slice 3: Mindful behavior pass, theme audit, then extend to Sleep-tab metrics. Spec: SPEC_sleep.md Section 13.
- [BUILT 2026-06-14, NEEDS DEVICE VERIFY] Recovery tab polish pass + REAL history backfill. (1) BURN ACCURACY now applied to Prev. Activity: new burnAccuracyPct load + adjustedSignals useMemo scales yesterdayActiveCal AND activCalBaseline by the user's pct so displayed kcal matches the rest of the app (hero + Key Signals both read adjustedSignals); recovery SCORE is unchanged because actScore is deviation-based (scaling both sides cancels). (2) UNIVERSAL 7d/30d header toggle now on the Recovery tab too (was Sleep-only); removed the per-card toggle from the recovery trend card to match the sleep tab's universal-only pattern. fetchRecoverySignals(baselineDays) parameterized + the signals effect re-fetches on range change, so 30D recomputes baselines over 30 days (deltas reflect the window). Key Signals labels now dynamic '{range}d avg'. (3) Hero gained a 'Change vs your {N}-day baseline' caption so the deltas are no longer unexplained. (4) Recovery Trend chart y-axis AUTO-SCALES (niceYTicks, mirrors Sleep Score Trend) instead of fixed 0-100. (5) Sleep/Recovery tab pills: active tab text now the cyan accent (theme.accentBlueRaw). DEV TOOL: Settings > Dev Tools > 'Backfill Recovery History' sets pj_dev_backfill_recovery; sleep.tsx consumes it on next Recovery tab open (mount effect) and recomputes REAL recovery scores for the last 30 days from actual Apple Health history -- fetchRecoverySignals(7, anchorDate) date-scopes RHR/resp/SpO2 to each past day's average (sleep-window / day discreteAverage) instead of 'most recent'; sleep score per night from fetchSleepHistory(30). Read-then-merges recoveryScore into each pj_<date> (never overwrites; days with no watch data skipped = honest gaps, no faked fill); a progress banner + ActivityIndicator shows during the run; Alert on completion + trendReload bump refreshes the chart. Burn accuracy intentionally NOT applied in backfill (score is scale-invariant; only the score is stored). SAFETY: pj_<date>.recoveryScore is read in exactly ONE place app-wide (the recovery trend loader) so backfill cannot touch Day Score/summaries/EvR. NOTE: synthetic/fake-fill idea was raised then rejected -- backfill is genuine recomputed history only. tsc clean on all touched files (useHealthKit.ts, app/sleep.tsx, app/settings.tsx, utils/recoveryScore.ts). FORWARD FLAG: when Recovery is later wired into Day Score, keep the locked going-forward freeze there -- this backfilled history feeds the trend chart only. REMAINING: device verify all; theme audit; judge the trend chart + auto-scale once 30d of real scores are backfilled.
- [FIXED 2026-06-14] Recovery Score showed "NAN RECOVER" + silently dropped sleep from EVERY recovery calc. Two bugs, one root: (1) app/sleep.tsx computed today's sleep `score` at line ~663, BELOW the recoveryResult useMemo (~line 565) that consumes it. Babel const->var hoisting made the memo read `score` as `undefined` every render, so sleep (22% weight) never reached calcRecoveryScore -- undefined*0.22 = NaN -> "NAN RECOVER". This is the same use-before-declaration tsc flagged earlier and we wrongly dismissed as "Babel makes it run fine." FIX: moved displaySleep/isManual/score computation ABOVE the memo. (2) utils/recoveryScore.ts guarded only `sleepScore === null`, so a NaN/undefined sailed through. HARDENED: every signal now requires Number.isFinite to count; sleep is a droppable input like RHR/resp/activity (Option B -- compute from available signals, renormalize by weights actually used); new minimum-data gate (need >=1 of sleep/HRV/RHR or NULL_RESULT); sleep contributor row drops cleanly instead of rendering "NaN". Verified on device: 88 sleep score now feeds Recovery (was 71 without it). Pure JS, no rebuild. OPEN (raised by Justin this session, NOT yet built): (a) CORRECTION -- the Recovery Trend chart is NOT a placeholder; the RecoveryTrendChart SVG component (sleep.tsx ~line 330) is fully built (axes, line, points, tap callouts, slide-in anim). The "builds over time / first data point" text is its EMPTY state, shown at 0 saved scores; once today's score persisted it renders the real chart. ONLY gap: it's locked to a fixed 0-100 y-axis where the Sleep Score Trend auto-scales (niceYTicks) -- apply the same auto-scale so the line fills the card (SPEC_sleep 11.7); (b) hero deltas (e.g. "-2ms") don't say what they compare to -- they're vs the 7d baseline but unlabeled; add clear "vs baseline" framing. Justin confirmed hero contents otherwise good; (c) UNIVERSAL 7d/30d range toggle pinned in the header like the Sleep tab (currently the toggle lives only on the Trend card) -- should drive BOTH the trend range AND the baseline window, which also resolves (b) (30D = deltas vs 30d baseline); (d) Prev. Activity (1028 kcal) ignores the user's burn-accuracy setting -- pulls raw HealthKit active energy, apply the same adjustment used elsewhere; (e) no 30d backfill by design (frozen going-forward like burnAccuracy/goalSnapshot).
- [HIGH] Recovery home card -- home screen has a Sleep card but no Recovery presence. Add a Recovery home card paired with the Sleep card: one linked home tile with two faces (Sleep / Recovery) that auto-scrolls/cycles on a timer so both always surface, manual swipe + auto-pause, each face taps into the matching hub tab. Full spec: SPEC_sleep.md Section 12 (design NOT locked -- interval, one-tile-vs-two, Recovery face contents, card-order integration, empty/limited + Mindful states all open). Build AFTER the Recovery tab polish (trend auto-scale, baseline-labeled deltas + universal toggle, burn-accuracy fix).
- [HIGH] Metric drill-down system -- tap any metric (Recovery hero row / Key Signal / Sleep metric) -> focused drill-down: that metric's 7/30d mini-graph + what-it-is / how-it's-calculated / what-it-affects / how-to-improve + baseline-or-range reference. ONE shared component + per-metric registry serving BOTH tabs (model on components/NutrientDrilldownModal.tsx). Solves Justin's "what are my culprits / how do I fix it" -- the tab currently shows numbers but never says what's dragging the score. Recovery is the priority target (barer); Sleep second (already has inline ranges/hypnogram/coach). Full spec: SPEC_sleep.md Section 13. DECISIONS LOCKED 2026-06-14: centered modal (CPP gate -- must fit comfortably + clean or revisit as full page); graph follows the universal 7d/30d toggle; Recovery metrics first (Sleep fast-follow); improve-tips curated STATIC but deterministically selected from the user's real value/trend (smart, not generic), AI later; SpO2 tappable but informational-only. Mindful: facts shown all modes, improve-tips softened/suppressed. Medical guardrail: improve-tips stay behavioral, never clinical (esp. SpO2/resp).
- [DONE 2026-06-14, NEEDS DEVICE VERIFY] Recovery zone thresholds calibrated -- PRIMED >= 80 / STEADY 60-79 / RECOVER < 60 (was placeholder 70 / 55-69 / 55). Bumped because a normal ~76 day read top-tier PRIMED, overselling it; PRIMED now reserved for genuinely strong 80+ days, a normal day reads STEADY. recoveryScore.ts label + zoneColor cutoffs; recoveryCoachTip positive band aligned to the 60 floor. Fixed cutoffs for v1 (personalized/percentile zones a possible later upgrade). No tooltip/tutorial number-sync needed (zones aren't cited by number in user copy). SPEC_sleep.md zone line updated. VERIFY: today's 77 should now read STEADY (amber), not PRIMED.
- [BUILT 2026-06-14, NEEDS DEVICE VERIFY] Sleep Metrics: diverging bars REMOVED, rebuilt as inline comparison rows -- every row is now big colored value on the right + small qualifier next to it + one muted subtext line, NO bars anywhere (Sleep balance bar killed too, for consistency). app/sleep.tsx renderMetrics; DivergingBar/BedtimeBar/BarTickLabel components deleted. COLOR FIX: Deep/REM above healthy range now reads GREEN (was amber -- the old `inRange ? good : warn` violated the locked rule "below = amber, in OR above = green"). Stage "vs norm" delta (e.g. "down 1% norm") is NEUTRAL grey so an arrow direction never contradicts the value's health color (Justin's catch). Bedtime: value colored by bedtime std-dev over baseline window (<=30 Consistent, <=60 Mostly steady, both green; >60 Variable amber), subtext = 10th-90th pct range. Sleep balance: surplus green / deficit red / on goal, magnitude + colored word, "vs Xh goal · last N nights". Wake events: at/below norm green, up to 2x norm amber, above 2x norm RED (Justin wanted a red tier for way-over nights), qualifier "up N above norm" carries the value color. sleep_hub tooltip: "Sleep debt" definition updated to "Sleep balance". Copy polish (device-gauged): "= norm" for the even case, "Goal 7h · Past 7 nights", "12m awake · Norm 3"; all arrow deltas neutral grey, only word-badges (Consistent/Surplus/Deficit/Variable) colored; right-column values 17pt. RANGE TOGGLE: one universal "Viewing Range" 7D/30D control pinned in the fixed header (under the Sleep/Recovery tabs, Sleep tab only) so it never scrolls away; no per-card toggles (tried per-card, Justin wanted universal-only after seeing it). REMAINING: device verify + Justin gauge; tune placeholder thresholds with real data; theme audit. OPEN QUESTION raised by Justin: Wake events is the only LAST-NIGHT metric in an otherwise range-averaged card + there's no 7d/30d awake trend anywhere; consider making Wake events a range-average ("Avg wake events" + avg awake minutes) for consistency and to surface awake-over-range. NOTE: copy/format sourced from Justin's planning-thread mockup (Option B inline arrow comparison).
- [BUILT 2026-06-14, NEEDS DEVICE VERIFY] Sleep Coach wired to Smart Coach Level 1 (sleep-scoped) -- replaces the one-night deterministic placeholder on the Sleep Hub Sleep tab. New computeCoachPacketSleep (smartTipsEngine.ts) mirrors the EvR variant but filters candidates to the sleep rule set (sleep_score_low/duration_short/bedtime_inconsistent/deep_low/score_high + sleep-headline cross rules cross_protein_sleep/cross_steps_sleep), no Family 9 branch, gates on >=3 sleep nights, positive "sleep steady" fallback. New refreshCoachTipSleep + loadCoachTipCacheSleep (coachAI.ts), 'sleep' surface + framing, cache key pj_coach_tip_sleep. sleep.tsx renderCoach now shows the AI-voiced tip via resolveTipBody (deterministic sleepCoachTip kept as the instant/offline fallback); lazy compute on hub open, one AI call/day, falls back to templated copy without an API key. Mindful suppression handled by the engine's applyMindfulSuppression. ADDITIVE ONLY: home/EvR/weekly/monthly coach untouched. sleep_hub tooltip gained a Sleep Coach definition; SMART_COACH_SPEC.md Level 1 surfaces updated. WINDOW FIX 2026-06-14: loadWindowDays got an optional startOffset (default 1 = yesterday back, unchanged for all existing callers); computeCoachPacketSleep passes 0 so the sleep coach INCLUDES last night and analyzes the same recent nights the Sleep Hub shows (was ending yesterday, which caused a confusing "6 of 7" when an older short night sat in the window and last night was excluded). Food/activity coaches still exclude today on purpose (incomplete at app open). DEV TOOL: Settings > Replay Sleep Coach clears pj_coach_tip_sleep + pj_coach_last_rule_sleep so the tip recomputes on next Sleep Hub open (tip is cached once/day). REMAINING: device verify (AI voice + fallback + Mindful), theme audit. NEXT SIBLING: Recovery Coach (Recovery tab) still deterministic -- needs HRV/RHR/resp/SpO2 fed into buildEngineContext + new recovery rules before it can wire the same way.
- Sleep Hub + Recovery Score -- full build specced in SPEC_sleep.md. Two-tab destination screen (Sleep / Recovery), new baseline-relative Recovery Score (6-signal weighted formula), replaces the old recovery subscore app-wide. DESIGN SESSION DONE 2026-06-12 (Decisions Log #11): file app/sleep.tsx, layouts locked, stage colors (Deep #0d9268 / REM #7e6bb3 / Core #3d6e9e / Awake #cc3333), readiness labels PRIMED/STEADY/RECOVER, zones green>=70/amber55-69/red<55 (placeholder), linear baseline scaling (placeholder), Mindful confirmed, v1 drops Activity Balance (prevDayActivity*0.17). HRV WIRING DONE + verified on device 2026-06-12 (62ms, SDNN most-recent; sleep-window scoping still TODO in the formula). EAS build LANDED 2026-06-12. BUILD IN PROGRESS. SLICE 1 SHIPPED 2026-06-12 + device-confirmed: app/sleep.tsx scaffold, "SLEEP & RECOVERY" header, Sleep/Recovery pill tabs, last-night Sleep Score hero (reuses shared components/SleepDonut.tsx extracted from index.tsx; same score math as home; Awake legend row via new sleepAwake token), Recovery tab placeholder. Home sleep card now navigates to the hub on full-card tap (gated off during edit; gear/feel/chevron keep their own taps) plus a corner chevron affordance. SLICE 2 SHIPPED 2026-06-12: fetchSleepHistory(days) in useHealthKit (buckets stage samples into nights by wake-day), animated Sleep Score trend line (7/30 toggle, SVG draw-in), per-night stacked stage-history bars (Core/Deep/REM/Awake, grow-in animation). Home card now has press-scale animation. SLICE 3 SHIPPED 2026-06-12: Sleep Metrics panel (wake events last night, bedtime consistency std-dev label, avg deep %, avg REM % over range) -- fetchSleepHistory extended with awakeCount + bedMin. CHART POLISH SHIPPED: both charts rebuilt as SVG with y-axis ticks, x-axis date labels, and tap-a-point/bar callout pills (matches StatsGraphCard); metric tiles colorized (deep=purple, rem=green, bedtime=consistency color). SLICE 4 SHIPPED 2026-06-12: Sleep Coach tip (mode-aware, suppresses corrective tips in Mindful unless mindfulGrowthAreas on) + exclude-last-night toggle (read-then-merge pj_<date>.excluded, filters both charts/metrics/coach via filteredHistory). POLISH PASS SHIPPED 2026-06-12 (Justin notes): left accent borders on all cards; fixed 7D/30D scroll-to-top (charts stay mounted on range switch); hero now matches the home Sleep card exactly (moon icon, 3-stage donut, awake as text line, no goal-dot line); trend avg moved into header as a real stat (no dot-joined footer); metrics rebuilt as clean rows + Sleep Debt; coach restyled as a tinted insight callout (still a deterministic placeholder, rewire to Smart Coach later); exclude demoted to a quiet footnote row. HYPNOGRAM SHIPPED 2026-06-12: new fetchLastNightSegments() in useHealthKit + Hypnogram component (4 lanes Awake/REM/Core/Deep, bedtime->wake timeline, PanResponder horizontal-drag scrub showing stage + clock time, vertical scroll preserved). Dropped sleep efficiency (Justin: score-creep). DESIGN-REVIEW ITERATIONS (device feedback): hero donut ditched for a compact "93 / Sleep Score" number (no big ring); moon icon removed; Last Night card centered (two hero numbers + stage legend spread across bottom) to kill dead space; hypnogram connectors removed (looked spiky/cheap) + lanes tightened to read connected; metrics show healthy ranges (deep 13-23%, REM 20-25%) with green/amber coloring so values mean something; "Last night" capitalized. MANUAL-SLEEP HANDLING: manual nights (pj_<date>.sleepOverride, feel-based score) now merge into the Sleep Score Trend so watch-less users get a trend; stage cards (Stages bars, Hypnogram, Avg deep/REM) show clear "needs Apple Health" empty states instead of blank. LATER ITERATIONS: /100 dropped from hero score; hypnogram moved INTO the Last Night card (under the two numbers, above bed/wake, own axis hidden) -- no separate Timeline card; Sleep Stages bars are now sleep-only (Awake removed) and tap shows the Core/Deep/REM breakdown (not total); wake-events metric row now shows total awake duration; (i) Sleep Hub tooltip DONE (tooltipRegistry 'sleep_hub' + TooltipIcon in header). RECOVERY TAB SHIPPED 2026-06-13: fetchRecoverySignals() in useHealthKit (sleep-window HRV average per spec Decision #7, 7-day baselines for HRV/RHR/resp/activity, SpO2 display); utils/recoveryScore.ts (new) with full v1 formula (HRV 35%, sleep 22%, RHR 18%, prev-day activity 17%, resp 8%; 3-signal fallback if no HRV: sleep/RHR/resp 40/40/20; both signals normalized when some missing; PRIMED/STEADY/RECOVER zones green>=70/amber55-69/red<55); renderRecovery() in app/sleep.tsx: hero card (large score + label + 5 contributor rows each with left-side status-colored pill, arrow+delta vs baseline, SpO2 display row, Limited data badge when no HRV), recovery trend card (reads pj_<date>.recoveryScore; first-day empty state; score auto-saved on each computation), key signals panel (HRV overnight / RHR / resp / SpO2 / prev-day active cal with 7d baselines as subs), recovery coach tip (mode-aware, same suppression pattern as sleep coach). Mindful: score always shown, corrective tips suppressed unless mindfulGrowthAreas on. REMAINING SLEEP TAB TODO: (1) Sleep Coach still a deterministic placeholder, rewire to Smart Coach engine later; (2) full theme audit (only Light/cyan seen); (3) no Sleep Hub interactive tutorial yet (data/tutorials.ts). REMAINING RECOVERY TODO: (1) wire Recovery Score into Day Summary, weekly/monthly summaries, EvR, Stats, dayScore.ts (spec Decisions #1/#8/#9); (2) Recovery Coach still deterministic placeholder, rewire to Smart Coach later; (3) theme audit; (4) tutorial entry.
- [BUILT 2026-06-13, NEEDS DEVICE VERIFY] Sleep Hub baseline spec + premium metrics rebuild -- SPEC_sleep.md Section 11. Personal 30d baselines (always last 30 synced nights, separate fetchSleepHistory(30) so the range-driven chart fetch is untouched; 7D/30D toggle only changes what charts/values DISPLAY, baseline is always 30d). baseline30 useMemo computes avg deep %, avg REM %, avg bedtime + bedtime SD, avg wake events; >=7 stage nights gate, else healthy-range/range fallback. history30 state + exclude scan extended. Sleep Metrics panel REBUILT (took several device iterations -- first dot-on-zones then plain fill bars both read vague). FINAL: every row a DIVERGING bar with a labeled axis (left/center/right hashes = the "numbers on the bars" ask), except bedtime. Avg deep/Avg REM (label keeps "Avg" -- these are RANGE AVERAGES, not last night; dropping "Avg" earlier made 12% avg deep look contradictory next to a 95 last-night Sleep Score) = diverging bar centered on your baseline %, but COLOR driven by medical healthy range (deep 13-23 / REM 20-25): in range green, too low OR too high amber, with caption "Below/Above healthy range (X to Y%)". Baseline = bar's center reference only, does NOT drive color (fixes the missing upper guardrail: 30% deep would have read green just for being above baseline). Sleep balance (renamed from Sleep debt) = diverging from goal(0), right=surplus/green left=deficit/red, symmetric auto-scaled +-Xh axis, value always real number ("2h 10m surplus"/"3h 15m deficit"/"On goal"). Wake events = diverging from your avg count (good side is LEFT, fewer=better; awake caption centered). Bedtime = neutral track + hash ticks at your early/late ends (10th-90th pct of 30d bedtimes, real clock times) + dot at typical LABELED "Baseline H:MM PM" under it (matches every other row's labeled reference -- the key fix; was an unlabeled solid green bar before). Components: DivergingBar + BedtimeBar + BarTickLabel (replaced MetricBar). No right-column subs; every secondary number is an axis label or one caption. Bars animate on mount. Pure JS, no rebuild. REMAINING after verify: tune placeholder thresholds with real data; full theme audit; tooltipRegistry 'sleep_hub' copy (Sleep debt->Sleep balance, baseline mention). NOTE pre-existing (NOT from this work): tsc flags `score` used-before-declaration in the recoveryResult useMemo (line ~640) -- Babel const->var makes it run fine; flagged for a later cleanup.
- [BUILT 2026-06-13, NEEDS DEVICE VERIFY] Sleep Score Trend y-axis auto-scale -- chart was hardcoded 0-100 so the line hugged the floor and looked flat. Now auto-scales to data min/max via niceYTicks (same nice-number algo as the Stats graph creator), so the line fills the card. app/sleep.tsx ScoreTrendChart. (Recovery trend chart still fixed 0-100 -- follow-up if this lands.)
- Hypnogram connector lines -- CUT PERMANENTLY. Tried twice (gradient step-down). Too sloppy when transitions are dense (short rapid awake slivers early in night create a tangled mess). No clean solution found.
- [SCOPED DOWN 2026-06-15] Card top accent gradient wash -- PULLED BACK TO SCORED-ONLY. The app-wide wash read as wallpaper in a card-heavy app (Justin's call); the wash only earns its place on hero/scored cards. KEPT on: Recovery hero, Sleep Score hero, day-summary category cards, DaySummaryModal. REVERTED home/stats/log/workout/faith/day-detail to their original look (no wash); stripped the accent washes from the Sleep/Recovery hub's non-score cards too. components/GradientCard.tsx (CardWash) stays as the reusable primitive. OPEN (Justin "go through later"): decide case-by-case if any specific pages/cards get the treatment back (candidates: stats Records + Trends graph cards, home faith hub, faith Gratitude + unify Today's Message card). Small/collapsed-card ugliness is moot now (no wash on small cards) but is the gating issue if we ever re-add the wash to smaller cards. Full history + decisions in SPEC_card_gradient.md.
- [DIRECTION LOCKED 2026-06-15, SPEC + PROTOTYPE PENDING] State-driven card wash ("gradient = a signal, not a skin"). Justin loves the gradient look but the app-wide version read as wallpaper because it was DECORATIVE and UNIFORM (same accent on every card). The fix: the wash appears only when a card has something to say, and the wash COLOR is the message. So the gradient stays selective and varies in color = no monotony, plus it carries information (Whoop/Oura principle: color = data, not decoration). Triggers (draft): scored cards -> status color (already shipped); goal-hit cards -> GREEN wash when the goal is actually met (steps / water / protein / calories), flat otherwise; optional over-target -> amber/red; possibly achievement/streak milestones -> celebratory wash. Most cards stay calm/flat most of the time; the wash is a meaningful highlight. Infra already supports it: components/GradientCard.tsx CardWash takes washColor + scored, so this is wiring TRIGGERS, not new plumbing. PLAN: (1) spec the full state-driven ruleset (which cards, which triggers, which colors, Mindful behavior); (2) prototype the safest high-value trigger first -- goal-hit GREEN on steps/water/macros -- and judge on device before expanding.
  - **DO NOT FORGET (Justin flagged this insight explicitly 2026-06-15):** goal-hit green was deferred earlier ONLY because every card already carried an accent wash, so adding green felt like piling more color on. That context is GONE -- the regular cards are flat now, so a goal-hit green wash would actually POP as a reward against the calm cards. The earlier "leave steps/water/calories flat for now" call should be REVISITED on that basis; it was situational, not a permanent no.
- [HIGH] [SUPERSEDED BY LINE ABOVE 2026-06-14] Card top accent gradient wash -- DECISION LOCKED: adopt app-wide, replacing the old left-accent-border card style. Reference impl SHIPPED on the 4 Recovery-tab cards (app/sleep.tsx, Justin-approved). The crisp top edge is baked into the gradient's first color stop (NOT a borderTopWidth -- that renders rounded corners unevenly on iOS). Wash = theme accent (default), Faith amber, status color on scored cards. Accent is subtle by nature (bg gradient is accent-tinted) -- accepted; pop comes from scored cards. FULL STRICT SPEC: SPEC_card_gradient.md (exact recipe, opacity variants, color rules, the shared-GradientCard refactor, the ~20-file swap list, 5-theme test reqs, shadow=TestFlight-only). REMAINING: Sleep-tab cards still on old style; build shared components/GradientCard.tsx + centralize card base style + swap all ~20 screens; shadow tuning on first TestFlight; 5-theme audit. A fresh thread can execute from SPEC_card_gradient.md cold.
- [DONE 2026-06-14] Sleep Score Trend gradient fill -- shipped as part of the trend-chart restyle (both Sleep and Recovery trend charts). Path area fill with a vertical fade (line color at ~0.2 opacity to transparent at the X-axis), mirroring the Stats graph. Went NEUTRAL (line color) rather than accent so the score-tier-colored dots carry the color. Now the de facto line-graph standard on the hub. app/sleep.tsx ScoreTrendChart + RecoveryTrendChart.
- [DONE 2026-06-13] Sleep Stages Y-axis gridlines equidistant -- rawMaxH rounds up to nearest step multiple. "10h" label gap fixed (template literal). app/sleep.tsx StageHistoryChart.
- [DONE 2026-06-13] Stage bar callout popup -- shows date + total header, Core/Deep/REM/Awake rows, Duration at bottom. Tapping popup dismisses it. app/sleep.tsx StageHistoryChart.
- [DONE 2026-06-13] Manual sleep footnote in Sleep Stages card -- "Apple Health data only. Manual sleep nights not included." app/sleep.tsx.
- [DONE 2026-06-13] Hypnogram Y-axis label color-coding -- Awake/REM/Core/Deep labels now match stage colors. app/sleep.tsx Hypnogram.
- [DONE 2026-06-13] Last Night score font size -- 46->38 to match duration text. app/sleep.tsx renderHero.
- [DONE 2026-06-13] Awake in Last Night stage legend -- added AWAKE Xm to Core/Deep/REM key row. Removed "Xm awake during night" subtitle. app/sleep.tsx renderHero.
- [DONE 2026-06-13] Wake events row -- count + awake time stacked right side, no left sub. app/sleep.tsx renderMetrics.
- [DONE 2026-06-13] Bedtime consistency interim fix -- shows avg bedtime anchored (+-Xm from HH:MM). Proper fix needs baseline spec session. app/sleep.tsx renderMetrics.
- [DONE 2026-06-13] Exclude card -- restored as own card below Coach with "OPTIONS" label. app/sleep.tsx.
- Sleep score stage weight tuning -- bump REM weight higher, soften deep sleep penalty. REM and Deep currently equal at 30pts each. utils/sleepScore.ts.
- Sleep edit disclaimer -- show disclaimer when user opens manual sleep edit: "will overwrite Apple Health synced data."

---

## COACHING & AI

- Smart AI extra nutrients -- decision needed: should sodium / vitamins / micros feed into Smart AI tips, or stay display-only? Working line: showing micro values is fine, but PRESCRIPTIVE advice on them is the medical/legal risk. Discuss before any build.
- AI Estimator label reading -- when the user photographs an actual labeled package (tub/box/bag) and gives a gram amount, the estimator should read the printed nutrition label instead of guessing. Justin shot his preworkout tub + stated grams and it still estimated. Ties to the unverified photo path. services/aiMealEstimator.ts.
- AI Meal Estimator -- BUILT & SHIPPING (app/ai-meal-estimator.tsx + services/aiMealEstimator.ts). Photo+text input, editable breakdown, universal + per-item portion multipliers, low-confidence flagging with hard-gate, today's-estimates failsafe (pj_ai_estimator_today), quota (free 3 / Pro 30, counts when result shown). AI badge on log rows + Edit Entry, hidden from Recents. Entry points ALL DONE: Food Library FAB, add-food header AI icon (slot-aware), log tab card (persistent, shown below the meals). tooltipRegistry + privacy.html done. Pro tier (isPro) hardcoded false until monetization ships (single swap point in ai-meal-estimator.tsx). REMAINING: interactive tutorial (deferred to tutorial audit session per spec); Edit Entry "1g" cosmetic (separate item below); voice mic in the description field is a placeholder (focuses the field/raises keyboard only) -- true in-app dictation: expo-speech-recognition (New-Arch-safe, chosen over @react-native-voice/voice which fails silently on New Arch) INSTALLED in the 2026-06-12 build with mic + speech permission strings; just needs JS wiring now (no rebuild). Meal Templates CUT (revisit later as "Save as Recipe"). Fast follow-on: Restaurant save concept.
- AI estimate Edit Entry display -- aiEstimated composite entries show "Servings 1 / 1g / Amount (g)" oddly in food-detail (it treats them as gram-based foods). Cosmetic only: macros log correctly and Update Entry preserves them. Fix: food-detail should render aiEstimated entries as a fixed "1 serving = whole meal" with no serving-size selector. Deferred, do not forget.
- AI Meal Estimator NOT YET VERIFIED ON DEVICE -- all testing so far was TEXT-ONLY. The entire PHOTO path is unexercised: camera capture, photo-library upload, image compression, the vision model actually reading a meal photo, the no-food detection (only fires with an image), and the photo_only / photo_and_text disclaimer variants. Also unverified: error states (network/malformed), quota-exhausted limit modal, Mindful-mode behavior (de-emphasized calories, "Record this meal", warmer disclaimer), and the full theme audit (only Light/cyan tested -- CLAUDE.md requires all 5 themes x accents before "done"). Verify these before TestFlight.
- AI Meal Estimator deferred-but-undocumented bits (now captured): (1) Toolkit/help launcher on the estimator screen -- spec wanted one per standard app pattern, not added; tooltip exists in Settings>Help but no on-screen toolkit. (2) Image compression is expo-image-picker quality-only (IMAGE_QUALITY 0.4); no hard max-dimension resize, so the under-1MB target is best-effort -- a guaranteed cap needs expo-image-manipulator -- now INSTALLED in the 2026-06-12 build, just needs JS wiring (no rebuild). (3) Offline handling relies on the fetch failing (lands in the network error state); there is no pre-flight connectivity check -- @react-native-community/netinfo now INSTALLED in the 2026-06-12 build, just needs JS wiring (no rebuild).
- [DIRECTION LOCKED 2026-06-15, SPEC WRITTEN, BUILD NOT STARTED] EvR full redesign -- EvR becomes the DIAGNOSTIC "why + what do I do" layer, not a fourth scorecard. Day/Weekly/Monthly summaries already own the scorecard job at every time scale; EvR was "a weekly summary you can resize," which is why it always overlapped. New model: EvR explains your results (good AND bad), names the lever, shows pointed proof on every card (claim + proof + lever), ranks a multi-card feed by RELEVANCE + anti-repeat instead of the weight-first ladder (fixes the "always weight" monopoly), uses per-pattern auto windows (no 14/30/90 selector), and spins positives when nothing is wrong (no empty state, no invented problems). The weight monopoly is one shared ranking function (selectByPrioritySpine) feeding ALL surfaces, so the relevance fix is recommended APP-WIDE (home + summaries + EvR); weight stays a dedicated scorecard card in summaries so nothing is hidden. Full blueprint: SPEC_evr_redesign.md. DECISIONS: [1 LOCKED 2026-06-16] ranking scope = APP-WIDE (build relevance/severity in the EvR card engine first, then promote to the shared spine for home + day/weekly/monthly; weight stays a dedicated scorecard tile in summaries so only the headline rotates). [3 LOCKED 2026-06-16] card migration: consistency_good -> lowest strength (never takes a slot); burn accuracy -> gated to deficit-mismatch only; positives -> capped at 3, ranked on the same 0-100 strength scale, result-explaining positives weighted ABOVE metric-high back-pats; recovery rules ported in as diagnoses. [2 = tune LIVE during build, not pre-settled] ranking weights tuned against the Dump EvR Cards output on real data. [4 PARKED] Free/Pro until surface locked. ALL BLOCKING DECISIONS SETTLED -- track 2 step 2 (styled surface) is cleared to build. ENGINE-SIDE OF STEP 2 BUILT + DEVICE-VERIFIED 2026-06-16 (buildDiagnosticCards): (a) card-migration decision 3 -- consistency_good dropped to lowest strength, burn_accuracy gated to deficit-lagging only, positives capped at 3 (verified: feed went 6->5 cards, logging back-pat dropped); (b) recovery rules ported in (verified via RECOVERY CHECK dump footer). STILL TODO in step 2: rebuild app/diagnostic-report-view.tsx to render the EvR headline + voiced card feed (drop old scorecard cards), cache voiced output per report, Mindful pass on the feed, drop the 14/30/90 window selector, tooltip+tutorial sync, 5-theme audit. THEN promote the relevance/severity ranking onto the shared spine (app-wide, decision 1). SURFACE PROGRESS 2026-06-16 (commits e249e36, 611346b, 50cc01d): diagnostic-report-view.tsx now renders the voiced card feed (claim + proof + lever) in place of the old scorecard finding cards (old finding-card components left defined-but-unused, delete in cleanup). Voicing now caches per report (voice once -> store voiced cards on the saved report -> reopen is instant; fixed the 5-8s-every-open re-voice). PROTEIN CARD = LOCKED REFERENCE for the full treatment: de-weighted claim (textSecondary 600), StatBar proof module (two big BebasNeue numbers value/target + labels + "Xg short" pill + animated fill bar), lever in tone color, CardWash gradient (scored/gentle variant, accent-toned, Justin approved on Light), and a dim corner watermark icon (clipped via inner overflow layer to preserve shadow; uses MaterialCommunityIcons food-drumstick -- Justin OK'd MCI for watermarks since Ionicons lacks a drumstick). ROLLOUT REMAINING (next session, mechanical now pattern is proven): apply StatBar + wash + per-topic watermark to every card. Three bar SHAPES needed (do not blind-copy value->target): (1) value->target higher-better (protein done; logging count/total); (2) value->100 score / positive cards -- MUST suppress the "short" delta and turn the second number into a caption e.g. sleep 88 over 14 nights (88/14 would be a broken 600% bar); (3) value-in-range band for fiber (25-38, no short/over when in range); (4) A-vs-B comparison bars for deficit (predicted vs actual) + recovery (after-easy vs after-hard). Per-topic icons: protein drumstick, fiber leaf, sleep moon, deficit/weight scale, logging checkmark/calendar. Then headline-dedupe (suppress feed card matching the coach headline), Mindful lever-suppression pass, drop 14/30/90 selector, tooltip+tutorial sync, 5-theme audit, delete dead finding-card components. NOTE: this reframes Smart Coach Level 2 -- the EvR "domain cards" become relevance-ranked diagnostic cards, not a fixed per-category readout.
  - SURFACE PROGRESS 2026-06-16 (session 2, committed): card visuals overhauled past the plain value->target bar after Justin flagged it was being force-fit to every shape. Final per-shape language (all in diagnostic-report-view.tsx StatBar): (1) TARGET fill bar -- protein, logging -- kept, plus a 0 + goal axis-tick row (AxisTicks helper); (2) SCORE bar -- sleep, sustained-low recovery -- value/100 fill + 0/100 ticks, second number is a caption ("over 14 nights"); (3) RANGE TRACK -- fiber -- a rail with the 25-38 healthy band highlighted + a marker pin + anchored axis ticks (0 / band edges / max), Justin LOVES this one and called it reusable (sleep-duration band, calorie window, goal-weight zone, HRV/RHR ranges = future reuse); (4) GOALBAR -- deficit predicted-vs-actual -- fill to ACTUAL with a grey tick at PREDICTED so overshoot = beat it (replaced an earlier dumbbell that read poorly when the two values were close; metric kind renamed dumbbell->goalbar); (5) COMPARE two-stacked-bars still on recovery load/sleep (recovery means surfaced from smartTipsEngine computeRecLoadDrag/TracksSleep as finding.compare) -- TODO: redo as proper paired bars. Engine: DiagnosticCard.metric extended with kind (target|score|range|compare|goalbar) + caption/rangeMin/rangeMax; per-card metrics emitted in buildDiagnosticCards. STRUCTURE: legacy Summary prose card CUT (only the needs-more-data notice kept); Coach Insight headline MOVED above the feed and restyled to the exact day/weekly/monthly blue box (translucent blue fill, centered header + divider, centered italic body -- NOT a gradient wash); gradient wash + per-topic corner watermarks (drumstick/leaf/moon/scale/calendar/pulse/flame/analytics) rolled to EVERY feed card (protein-only gate dropped). All tsc-clean. STILL TODO: recovery paired bars; positive cards get a mini ring gauge (decided, not built -- so positives aren't the only visual-less card); momentum card engine fix [DONE 2026-06-16, device-verified via new MOMENTUM CHECK dump footer]: surplus_nextday rewritten FREQUENCY-LED, not median (Justin's point: median is unstable on a 4-5 sample). Fires only when >=5 over-target days in a fixed 30d window AND >=60% snowballed into another high day; magnitude is a trimmed-mean overage (single biggest next-day dropped) shown as secondary garnish only. CRITICAL #2 FIX folded in: "over target"/"ran high" now judged vs each day's FINAL goal = base calTarget + that day's active calories (burn-accuracy adjusted), NOT the flat target -- the old card was a FALSE POSITIVE that fired (+158) only because it ignored activity, making normal training-day eating look like overeating. Audit confirmed this flat-target miss was ISOLATED to this one rule; summaries/dayScore/EvR-deficit already apply burn accuracy correctly; recovery uses no calorie goal. Visual = a 'dots' metric kind (frequency row: one dot per over-day, filled=snowballed) rendered in StatBar. On Justin's REAL data the card correctly does NOT fire (1 over-day in 30, 0 snowballed) -- validates the fix killed a false positive. Verified via Dump EvR Cards MOMENTUM CHECK footer (over-days/ranHigh/ratio/overage/fires). NOTE: his data won't trigger it, so the dot-row VISUAL is uneyeballed on-device (offered a temp seed, Justin TBD). headline-dedupe (suppress feed card matching the coach headline); Mindful lever-suppression pass; drop 14/30/90 window selector; tooltip+tutorial sync; 5-theme audit; delete dead finding-card components (FindingCard/ConsistencyCard/DeficitCard/BurnAccuracyCard/MacroCard/SleepCard still defined-but-unused in diagnostic-report-view.tsx).
  - SURFACE PROGRESS 2026-06-16 (session 3, committed): closed out several of the rollout items with Justin. (1) DATE LABELS -- "AS OF <date>" in the viewer didn't sit right with Justin -> changed to "GENERATED <date>"; the entry-screen saved-report rows still showed the OLD windowed date range ("Mar 18 - Jun 15, 2026") which was a leftover from the dropped window selector -> now show "Generated <date>" from report.generatedAt (diagnostic-report.tsx + diagnostic-report-view.tsx). KNOWN minor (Justin flagged, left as-is): two reports generated the same calendar day both read "Generated <same date>" (IDs are unique to the second, so they're distinct objects; could add time-of-day later if it bugs him). (2) RECOVERY PAIRED BARS -- DEFERRED, not built: Justin's real recovery is stable so those cards never fire, nothing to eyeball; revisit only when data triggers them. (3) POSITIVE-CARD MINI RING -- KILLED off the list: the per-shape StatBars already give positives a real visual (deficit goalbar, sleep score bar, fiber range track all render on his positive cards per screenshot), so the "positives are the only visual-less card" premise is gone. (4) HEADLINE-DEDUPE -- DECIDED AGAINST (keep as-is): Justin's call + agreed -- the blue Coach Insight is a conversational paragraph, the cards are visual proof + action; same topic in both reads as headline->detail, not duplication; deduping would orphan the headline. (5) DEAD CODE -- DONE: deleted FindingCard/StatusPill/DataRow/ConsistencyCard/DeficitCard/BurnAccuracyCard/MacroCard/SleepCard + dead helpers windowLabel/fmtLbs + dead noteText style + the now-unused finding-type imports (BurnAccuracyFinding/ConsistencyFinding/DeficitFinding/FindingStatus/MacroFinding/ReportWindow/SleepFinding) from diagnostic-report-view.tsx. tsc clean on both touched files. (6) MINDFUL LEVER-SUPPRESSION PASS -- [BUILT 2026-06-16, NEEDS DEVICE VERIFY in Mindful]: in DiagnosticFeedCard, a CORRECTIVE card in Mindful now flips its amber/red accent to neutral accent-blue (same language as the Recovery divergent bars in Mindful), the CardWash + StatBar inherit that blue so the card reads calm not alarming, and the lever drops the "->" arrow + bold directive color and becomes a quiet ITALIC note in textSecondary (observe, not instruct). POSITIVE cards untouched in every mode (green stays, arrow stays). Chip already hidden in Mindful. The AI voicer ALSO softens the copy in Mindful (confirmed FEED_VOICE_MINDFUL_NOTE in coachAI.ts strips directive language). KNOWN EDGE (flagged to Justin, left as-is): voiced cards are cached/persisted onto the report (the fix for the 5-8s re-voice), voiced under whatever mode is active at FIRST view; so generate+first-view in Balanced then switch to Mindful = visual softens correctly (blue/italic/no-arrow) but the cached TEXT stays Balanced-voiced. Re-voicing on every mode switch would resurrect the slow re-voice cost; left it. REMAINING after this: device-verify the Mindful card treatment (Justin to test); then drop 14/30/90 window selector [already DONE per the per-pattern-windows line above -- confirm nothing's left]; tooltip+tutorial sync; 5-theme audit; THEN promote relevance/severity ranking onto the shared spine app-wide (decision 1, the last big EvR item).
- [DONE 2026-06-16, DEVICE-VERIFIED via candidate dump] Headline topic fatigue (anti-weight-monopoly). The shared ranking spine (selectByPrioritySpine) now accepts an optional recentTopics list and downranks a candidate whose headline TOPIC led on recent computes (penalty 2.0/1.2/0.6 for 1/2/3 computes ago, decays out over ~3 days). net/weight/cal rules share one "weight" topic bucket so the weight/calorie STORY stops repeating. Downrank-only (a fatigued topic still shows if it is the only finding); family ladder kept underneath. SAFETY: recentTopics defaults to [] = byte-identical to the old spine, and ONLY the home card (computeCoachPacket surface 'home') passes it -- EvR, day/weekly/monthly summaries, and the scoped sleep/recovery coaches are completely untouched (they pass nothing). New additive AsyncStorage key pj_coach_topic_hist_home (read-then-merge, last 3 topics). tsc clean (only pre-existing unrelated errors elsewhere). VERIFY: rotation builds over consecutive days of home computes (history must accumulate) -- to see it immediately, seed pj_coach_topic_hist_home with ["weight","weight","weight"], reset the coach cache, open home; the headline should lead with a NON-weight finding if one exists.
- [DONE 2026-06-16, DEVICE-VERIFIED via candidate dump] Extended headline topic fatigue to the remaining multi-topic surfaces: EvR (computeCoachPacketEvr, key pj_coach_topic_hist_evr shared across windows), Weekly (pj_coach_topic_hist_weekly, last 3 weeks), Monthly (pj_coach_topic_hist_monthly, last 3 months). Same proven mechanism as home, same downrank-only behavior, each new additive key. So the weight monopoly is now broken on EVERY multi-topic coaching surface. Scoped sleep/recovery coaches still exempt (single-domain by design). tsc clean. KNOWN MINOR: EvR pushes per window, so opening 14 then 30 same day can double-record a topic (mild over-fatigue) -- moot once the EvR window selector is removed in the redesign. DEFERRED (was the other half of 1b): the full strength-weighted relevance-score rewrite (needs every rule to expose a severity signal) -- intentionally pushed to the EvR card rebuild (track 2) where it is safer to do with proof-data per card rather than bolted onto the live spine blind. NEXT: track 2 (EvR surface rebuild: diagnostic headline + claim/proof/lever card feed). VERIFICATION 2026-06-16: added a read-only "Dump Home Coach Candidates" dev tool (Settings) that mirrors the home spine and prints each candidate's family/tier/fatigue/score. Confirmed on real data: seed reads (recentTopics [weight,weight,weight]), penalty lands (fat2.00 on both weight rows). Weight still leads home ONLY because Justin's 7d window has 0 non-weight corrective candidates -- the sole alternatives are family-6 positives at score 6.20, and a penalized weight tip (3.00-3.20) still beats them. This is downrank-only behaving as designed, NOT a bug: a ~2-family penalty can't reach across to family 6. It also PROVES why anti-repeat alone cannot break the weight monopoly on a single-slot surface when a user is dialed in -- the structural fix is track 2's strength-weighted relevance/severity rewrite (replace the rigid family ladder), which validates that deferral. FOLLOW-UP RESOLVED 2026-06-16 (NOT a bug): protein_under is the #1 corrective in the 30d EvR feed (avg 117g < 164g goal) but absent from the home 7d candidates. Extended the dump with a PROTEIN CHECK block; on real data home's goal reads 164g (same as EvR, no goal bug) and the last 7 days protein = [131,94,102,155,171,167,165] = ~141g avg, only 3 of 7 days under the 131g (80%) threshold (pattern needs >=4). So home correctly stays silent (recent week is strong) while EvR correctly flags the 30d average (dragged down by older days outside the 7d window). Window-scope difference, both correct -- a live example validating the redesign's per-pattern windows (short "recent looks fine" vs long "30d trend under" are different true stories). Do NOT patch the home rule to match EvR; it would make home less accurate about the recent week. Both diagnostic dev tools (Seed Topic Fatigue, Dump Home Coach Candidates) kept until the EvR surface ships, then removable.
- [DONE 2026-06-16, NEEDS DEVICE-VERIFY via Dump EvR Cards] EvR per-pattern windows -- the 14/30/90 selector is GONE (Justin's pain: regenerating multiple windows). generateDiagnosticReport() now takes NO window arg: it loads the max (90d) once and each finding slices its own lookback. DESIGN SPLIT (deliberate): the metric AVERAGES get per-pattern windows because recency matters (a recent protein dip must not be hidden by good months) -- protein 14d, sleep 14d, fiber 28d, consistency 30d, deficit/weight 90d, momentum 30d, recovery 14d (separate). The cross-signal CORRELATIONS (sleep->intake, burn->intake, weekend, water, sodium, steps->sleep, workout/rest) run on the FULL 90d pool because pattern detection benefits from more pairs, not fewer -- this is a principled call, not a shortcut. Each card states its OWN window in its proof ("Over 14 days", "Over 90 days", etc.). Implementation: window constants `W` + `MAX_WINDOW`/`MIN_TOTAL_LOGGED` in diagnosticReport.ts; slices daysB(28)/daysR(14)/daysC(30) off the 90d load; loggedDayData stays 90 (deficit + correlations), loggedB/loggedR added; sleepDaysAll(90, correlations) vs sleepDays(14, finding avg); buildDiagnosticCards now takes a `windows` object instead of windowDays and labels each card's window. GATE CHANGE: insufficientData is now a single floor (>=7 total logged across 90) instead of per-window minDaysForWindow -- a finding just stays silent if its slice lacks data; the whole report no longer blocks on one window. SURFACES: entry screen (diagnostic-report.tsx) dropped the picker + date-range pill -> single "Generate Analysis" button + history (saved-report rows drop the "-day window" tag; most-recent = CURRENT); viewer pill now "AS OF <gen date>" not "30-DAY · dates"; dump tool (settings.tsx) is one action not a 14/30/90 picker. New reports store windowDays=90 (still a valid ReportWindow, keeps old reports rendering). Tooltip+tutorial synced SAME SESSION: removed the dead "PICK A WINDOW" tutorial step (evr_window_picker target gone), reworded GENERATE step, replaced the "30-day window" tooltip with "Each pattern has its own timeframe". tsc clean (only pre-existing unrelated errors). VERIFY ON DEVICE: run Dump EvR Cards (now single action) -- each card should show its own window label, protein/sleep should reflect the recent ~2wk, weight the ~90d; confirm nothing broke vs the last 30d report. KNOWN: behavioral correlations labelled "Last 90 days"/"Across your recent weekends"; if a correlation feels like it should be shorter we can scope it to daysB later. The old EvR-window double-record topic-fatigue minor (noted above) is now MOOT (no per-window pushes).
- Smart Coach Level 2 (Focused Tips) -- single-metric AI-voiced tips for home card slots 2+ and EvR domain cards. NOTE 2026-06-15: the old "Day Summary surface assignment" blocker is RESOLVED (SMART_COACH_SPEC.md Open Items #1: Day Summary stays Level 1, 1-day window, no changes). EvR Level 2 is fully specced and unblocked, BUT the EvR side now lands inside the EvR redesign above (relevance-ranked diagnostic cards) -- build Level 2 home slots and EvR cards in step with that. The one genuinely-open Level 2 item otherwise is Weekly/Monthly summary screen layouts (not an EvR dependency). SMART_COACH_SPEC.md.
- Caffeine tracking -- daily total, high-amount warnings, first-use disclaimer (includes pregnant women guidance). Design decisions needed: quick-add vs food field, thresholds. Duty-of-care item.
- Food group pattern detection -- if user logs zero whole foods X consecutive days, surface gentle tip. Mode-aware.
- NEAT definition -- one tooltipRegistry.ts entry only. No UI changes. Shows in Settings > Help automatically.

---

## STREAKS

- Burn accuracy freeze -- changing burn accuracy retroactively shifts streaks. Freeze burnAccuracyPct per-day same as goalSnapshot. utils/goalHit.ts / stats.tsx.
- Streak grace day system -- mode-aware grace days for fitness streaks (water/steps/calories/etc.). Discipline: earn, cap 1. Balanced: earn, cap 3. Mindful: no punishment.
- Streak end warning visuals -- color shift on streak card to orange/red when within X hours of midnight, action not done.
- Edit Streak Count -- manual override with disclaimer. Design session before building.

---

## TUTORIALS & TOOLTIPS

- [HIGH] TUTORIAL + TOOLTIP FULL AUDIT -- in progress, tab by tab (batches of 3, device-tested each).
  - DONE: return-nav framework fix (engine now auto-captures launch route, returns there on end/skip only if a step navigated away; explicit returnRoute still wins). GO_BACK error fixed (canGoBack guards on tutorial-action router.back calls in add-food/recipe-builder/workout-library).
  - DONE: LOG TAB content audit -- all 7 tutorials (log_food, manage_log, barcode, create_food, recipes, log_edit_layout, if_card). Fixed: log_food steps 7/8 dead spotlight (food-detail ScrollView now registered), manage_log step 4 (scrollToTop), create_food macros step (noDimOverlay + scroll), recents count 20->15, ~43 double-dashes across all mode variants.
  - DONE: WORKOUT TAB content audit -- all 4 tutorials (workout_basics, programs, routines, exercise_library). Fixed: programs + routines rebuilt from static info-walls into real interactive tours (deep-link via new tutorialTab param to workout-library Programs/Routines tabs; new spotlight targets: tab row, first program card + LOAD PROGRAM btn, My Routines header, first preset routine card; registered routines ScrollView for scroll-to). Dropped stale STARRED ROUTINES step (no star UI exists). Stale fact Rest Heavy->Rest Day Heavy. ~20 double-dashes across all mode variants. Device-tested all 4. Committed 9078056.
  - DONE: HOME TAB content audit -- all 8 tutorials (meta, meta_mindful, edit_layout, cal_card, macros_card, sleep_card, sleep_card_manual, yvy_card). Copy-only pass: 26 double-dashes across all mode variants (sleep/yvy were already clean). Wiring verified green (all targets register in index.tsx, both edit-layout actions register, home ScrollView registered). Stale facts verified accurate, no changes: cal thresholds (+/-50/149/150), edit-layout badge colors (red=showing/green=hidden), sleep point splits (40/30/30 staged, 60/30/10 feel-based). User device-tested the 5 visible tutorials.
  - DONE: STATS TAB content audit -- all 4 tutorials (graph_creator, streaks, effort_vs_results, day_score). BUG FIX: graph_creator empty preview (demo graph + creator preview read trendDataMap['7'] which was never loaded when active period != 7; injectTutorialGraph now loads the 7-day series, mirrors openCreatorModal). BUG FIX: EvR patterns dead-spotlight (section returns null when no cross-signal insightTips; added skipIfTargetMissing to evr_correlations + evr_suggestions so they skip gracefully; NO structural EvR changes, Smart Coach L2 overhaul coming). Stale facts: graph 19->17 data types (grid renders all of DATA_KEY_META = 17), EvR "9 correlation patterns" reworded (engine has 7 cross-signal rules, shows max 5; dropped brittle number). Verified accurate: 8 colors, 14 streak presets, Day Score 40/35/25 weights. ~24 double-dashes cleaned across all mode variants. User device-tested all 4 + both bug fixes.
  - REMAINING: faith_and_style (1, tab=profile -- the "Your Style & Faith Journey" tour; NOTE the Faith tab itself has zero tutorials, possible backlog item). Spotlight lag (TestFlight verify). Hidden-card guard (home tab hideable cards). Log Today's Total interactive tutorial (separate build). Tooltip audit + wording passes. data/tutorials.ts + tooltipRegistry.ts.
- Tooltip audit pass -- sweep all cards, flag every card missing a (i) tooltip, build missing ones.
- Tooltip wording polish pass -- copy quality pass. Known issues: Active (Apple Health fallback language), Remaining (algorithm vs description), Net (explain BMR first), Color Coding rewrite, em-dash sweep.

---

## ANIMATIONS

- Number transitions -- AnimatedNumber component shipped. Home: calories, macros, steps, water, weight. Log tab: Today's Total calories + macro bars. Remaining: stats tab values, workout tab reps/sets.
- Progress bar/ring/donut animation audit -- check which are already animated vs missing. Targets: calorie bar on load, macro bars on log entry, food log donut on load, water bar bounce on update, sleep donut on load.
- Goal moment animations -- water goal hit (bar fills + pulse), step goal hit (steps flip green), calorie goal hit (color transition).
- Achievement Toast phase 2 -- 12 badge animations: water (filling), steps (footprints), weight (scale needle), streak (flame), first workout (dumbbell), sleep (moon/ZZZ), calorie (plate clearing), IF (clock hands), Bible verse (page turn), journal saved (pen stroke), morning intention (sun rising), workout checked off (card pulse).

---

## SETTINGS & MODES

- Settings/Help: Coaching Style + Faith Journey in-depth explainers -- two-tier: quick blurb from setting row + full article in Settings > Help. UI approach TBD.
- Style/mode audit -- full pass: features that shouldn't show in Mindful, features that should be Discipline-only, wrong defaults. Dedicated session.
- Mindful mode full app-wide audit -- inconsistent implementation. Day Summary, every screen, card, and copy string. Dedicated session.
- Goals sub-category accordion polish -- FITNESS GOALS + NUTRITION GOALS sub-sections collapsible. settings.tsx.
- Resources and wellness links -- curated section in Settings > Help. Christian/health/wellness books, YouTube channels. Mostly static. Design session.
- "You've grown" coach message -- after key thresholds (weight milestones, streak lengths, logging consistency). Mode-aware. Ties to faith journey prompts. Design discussion first.

---

## JOURNAL

- Journal icon on Stats tab header -- not present. Routes to journal.tsx.
- Date on journal entries tappable -- routes to that day's Day Detail. Not implemented.
- Search within journal entries -- low priority.
- Long text stress test -- verify 500-word entries format correctly. QA task.
- Multiple entries same day -- verify prayer + gratitude same day display correctly. QA task.

---

## NOTIFICATIONS

- Notifications spec build -- SPEC_notifications.md is the source of truth. 14 types, copy pools, deep linking all specced. Build in dedicated session. Deep linking bugs fixed 2026-06-10 (water/weight scroll, activity routing, IF tab mismatch).
- Notification center -- bell icon in profile header, badge, real-time toasts for Health sync events.
- Daily summary push notification -- push version of morning Day Summary pop-up. NOT in the 14-type spec yet; add before building.

---

## VISUAL POLISH

- Full theme audit -- all 5 themes x all accent options, every screen, before beta. Dedicated testing session.
- Progress bar track color pass -- sweep track colors across all themes.
- Empty state illustrations -- SVG illustrations replacing icon + text empty states. Theme-aware, consistent style. App-wide.
- MFP switcher experience -- first-impression UX for power users arriving from MyFitnessPal.
- Sign-in logo entrance animation -- logo pops instead of fading. Verify on TestFlight before investigating.

---

## INFRASTRUCTURE

- Firestore migration -- move primary data from AsyncStorage to Firestore. Auth (Apple + Google) already done. Big infrastructure item.
- State restoration on launch -- save active tab + scroll position, restore on cold launch.
- HealthKit source detection -- read source identifier to show "via Garmin/Whoop/Oura" labels on sleep/HRV data.
- Offline first behavior.
- In-app review prompt -- prompt to rate at the right moment.
- Accessibility -- respect system Dynamic Type font sizes.
- Tooltip pulse visibility awareness -- only pulse when card is visible in ScrollView viewport. Complex, not blocking.
